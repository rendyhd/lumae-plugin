# Lumae plugin — work status

The single source of truth for the plan in [docs/plan/LUMAE_FINAL_PLAN_2026-09-24.md](plan/LUMAE_FINAL_PLAN_2026-09-24.md). There is one line per work package (WP). Work packages land on a phase branch, and each phase has one PR to `main`. The older `docs/remediation/RESUME.md` and ledger are kept as historical records.

## Current state

| Item | State |
|---|---|
| `main` release policy | LumaeAnalysis pinned to the 1.2.5 archive (P0-1). Merges do not publish. |
| CI on `main` | The `test` job (PG17) runs on every PR push. Making it a required check is pending the user (U1). |
| Next release | 1.3.0 (P4-1). Phases 1–3 are merged (Phase 3: rendyhd/lumae-plugin#8, e88dabd). Follow-ups run on `phase/3f-followups`. User decision (2026-09-26): release only when everything is fine **and** the matching Lumae app (Auralscape) changes are done; then U5. |
| Phase 2 exit gate (2026-09-25, `phase/2-performance` 66ec4c3) | `run_baseline.py --check` at scale 1 (94k profiles with real edges; [docs/perf/PHASE2-EXIT-2026-09-25.json](perf/PHASE2-EXIT-2026-09-25.json)): health p95 3.6 ms (≤50), settings 6.5 ms (≤100), projection no-change 3.0 s / 357 MB (≤5 s / 400 MB), delta 6.2 s (≤10), v2 create 2.1 s with 5.2 ms global lock (≤5 s / 50 ms), page p95 32 ms (≤50). The publication critical section is 6.0 ms against ≤5 ms, an accepted miss (P1-2 decision, 8.2 ms then; re-check after P3-2). AUD-07 (projection) and AUD-08 (health/settings) are inverted. End-to-end gate: [docs/perf/E2E-2026-09-25.md](perf/E2E-2026-09-25.md); the same-source creator decision is in the plan's P2-4 entry. Full suite 859 passed, 1 xfailed. |
| Phase 3 exit gate (2026-09-26, `phase/3-semantics`) | `run_baseline.py --check` at scale 1 ([docs/perf/PHASE3-EXIT-2026-09-26.json](perf/PHASE3-EXIT-2026-09-26.json)): health p95 4.5 ms (≤50), settings 26 ms (≤100), publication 5.0 ms (≤5), projection delta 7.9 s (≤10), v2 create 3.5 s with 13 ms global lock (≤5 s / 50 ms), bootstrap page 42 ms (≤50). **Projection no-change 5.15 s misses ≤5 s**: not a Phase 3 regression. `catalog_analysis.py` is unchanged since Phase 2; the Phase 2 code (origin/main) measures 5.5 s on the same database; dropping P3-5c's `search_text` column changes nothing (5.1 s). This container is slower than the one that measured 3.0 s at the Phase 2 exit (follow-up: widen the margin or re-measure on a quiet machine). The exit also fixed two issues. Settings p95 had grown from 7 to 58–75 ms: P3-9's edge panel scanned `edge_profiles` twice per poll, and it now uses one index-only scan (d6e1b27). CI (PG17) had been red since the P3-6 merge: a batch delete answered in `RETURNING` order, which is undefined, and it now answers in request order (d8a40b8); a P3-5c test assumed C-collation order (d6e1b27). |
| Client hand-off | [docs/handoff/AURALSCAPE_SYNC_HARDENING_HANDOFF.md](handoff/AURALSCAPE_SYNC_HARDENING_HANDOFF.md) |

## Work packages

| WP | State | PR | Merge SHA | Tests |
|---|---|---|---|---|
| Docs: audit, plan, hand-off | merged | rendyhd/lumae-plugin#2 | 629089f | docs only |
| P0-1 CI pinned artifact | merged | rendyhd/lumae-plugin#3 | 746a7b8 | 573 passed; retro review PASS |
| P0-5 status and doc hygiene | merged | rendyhd/lumae-plugin#4 | 5766d68 | docs only; retro review PASS |
| P0-1/P0-5 review follow-ups | merged | rendyhd/lumae-plugin#5 | 38c11a4 | release tests strengthened |
| P0-4 sync contract | merged | rendyhd/lumae-plugin#5 | 38c11a4 | docs only; review fixes applied |
| P0-2 migrated_db fixture | merged | rendyhd/lumae-plugin#5 | 38c11a4 | 582 passed |
| P0-3 perf harness and baseline | merged | rendyhd/lumae-plugin#5 | 38c11a4 | baseline: 6/7 budgets missed (expected) |
| P1-1 no-op republish keeps edges | merged | rendyhd/lumae-plugin#6 | 9552f43 | 596 passed |
| P1-4 gzip transport and private headers | merged | rendyhd/lumae-plugin#6 | 9552f43 | 628 passed; 50-row edge page 1002 KB → 372 KB (2.7×, distinct edges, gzip level 4) |
| P1-2 journal compaction and retention | merged | rendyhd/lumae-plugin#6 | 9552f43 | 607 passed; publication p95 20.9→6.2 ms (P1-2 alone), 8.2 ms with P1-1 |
| P1-7 single-snapshot /changes reader | merged | rendyhd/lumae-plugin#6 | 9552f43 | 674 passed |
| P1-3 fences and 1.3.0 | merged | rendyhd/lumae-plugin#6 | 9552f43 | 696 passed |
| P1-5 v2 snapshots store edge references | merged | rendyhd/lumae-plugin#6 | 9552f43 | 705 passed; create@94k 8.42 s (413; 115.7 s / 2720 MB WAL with limits lifted) → 3.0–4.3 s / 73–75 MB WAL; page p95 31.4→24.8 ms (50 rows); global lock hold 3–4.3 s left for P1-6 |
| P1-6 v2 operability | merged | rendyhd/lumae-plugin#6 | 9552f43 | 737 passed; create@94k 2.87 s, global lock 5.1 ms (same fixture before: 2.95 s, 2946 ms); page p95 31.0 ms |
| P2-7 FederatedAlbums proxy timeouts | merged | rendyhd/lumae-plugin#7 | df4d7c8 | 598 passed |
| P2-2 incremental projection | merged | rendyhd/lumae-plugin#7 | df4d7c8 | 613 passed, 1 xfailed; no-change 30.5→4.0 s (1,596→356 MB), delta 65.0→5.8 s |
| P2-1 committed status summary | merged | rendyhd/lumae-plugin#7 | df4d7c8 | 790 passed, 1 xfailed; health 349→4.2 ms, settings 496→6.9 ms (p95); no-change projection 2.45→2.7 s |
| P2-5 migration lock hygiene | merged | rendyhd/lumae-plugin#7 | df4d7c8 | 803 passed, 1 xfailed; re-run migrate on a populated schema: ACCESS EXCLUSIVE on 17 tables and SHARE on 16 (26 distinct) → no lock above ROW EXCLUSIVE; fresh and upgrade schema identical to base |
| P2-3 catalogue lock coupling | merged | rendyhd/lumae-plugin#7 | df4d7c8 | 830 passed, 1 xfailed; catalog_state hold @20k 147,086→679 ms (median of 3 probed runs; full reconcile of 132k tracks with 20k withdrawals; publisher-side max 149,417→734 ms); post-commit edge sweep at 94k 175 ms (no orphans), 1.1–2.3 s (20k withdrawn) |
| P2-8 FederatedAlbums migration locks | merged | rendyhd/lumae-plugin#7 | df4d7c8 | 811 passed, 1 xfailed; re-run migrate: ACCESS EXCLUSIVE and SHARE on 3 FederatedAlbums tables → no lock above ROW EXCLUSIVE; shared-cluster advisory pg_locks probes filtered by database |
| P2-6 end-to-end scale gate | merged | rendyhd/lumae-plugin#7 | df4d7c8 | 803 passed, 1 xfailed; 94k profiles + edges on gunicorn gthread 1×4 and the stock AudioMuse 8aa1639c host: v2 first load 236 s / legacy 215 s (785 MB gzip), no-op re-analysis 0 events, kill -9 at 6 points, 2nd device PASS; health/settings p95 ≤ 44 ms during loads; LUM-005/K6 pending P3-2 (8.3 KB/track); 2 concurrent creates p95 6.7–9.7 s (1 × 503) → P2-4: implement SQL-side capture; capped catch-up 1,056,000 events 48 s; found v2 page plan bug (docs/perf/E2E-2026-09-25.md); review follow-up: 8 kill points incl. both captures + K5 retry, direct edge-row checks, fixture guard, verified at 0.02 with 2 mutations caught |
| P2-4 v2 capture in SQL | merged | rendyhd/lumae-plugin#7 | df4d7c8 | 849 passed, 1 xfailed; snapshot and first catch-up rows built by INSERT … SELECT (off the web worker's GIL), output byte-identical to the Python capture (oracle equivalence test); create@94k 3.65→2.35 s; e2e 2 creators p95 same source 8.47 s (1 × 503)→4.85 s (0 × 503), different sources 6.55→2.41 s, status routes p95 during creates 35/50/68→12/18/27 ms; capped first catch-up unchanged (1,056,000 events: review re-measure 42.6 vs 42.8 s; the 50.5→39.7 s in the P2-4 commit message did not reproduce); page queries limit before the edge lookup (fresh table, 1,880 edges: 162 ms / 1,880 lookups→5.5 ms / 50) |
| P2-4b capture follow-ups | merged | rendyhd/lumae-plugin#7 | df4d7c8 | 859 passed, 1 xfailed; profiles whose ref_lufs is not plain (NaN/inf, |x| ≥ 2^23, non-zero below 1e-4) are serialized by serialize_profile itself, fixing review LOW-1 (efd=-2: 503 → null) and LOW-2 (7.038531e-26 → 7.0385313e-26); the float4 shortest-decimal search is gone; plain-range float verification 0 mismatches at efd 1/0/3/-5, exhaustive double-rounding scan of the plain range 0; create@94k (boot_bench ×3) 2.12/2.69/1.96 → 2.12/2.04/2.18 s, 0 fallback rows on analyzed loudness; catch-up number assumptions documented; contract lists every hold lapse as a 410 cause; test for changes between snapshot batches (LOW-7); e2e: capture-kill lock always released, bounded 429/503 retries, cut-off requests matched one to one, git_sha from describe --always --dirty |
| P3-13 LUM-001 concurrency test strength | merged | rendyhd/lumae-plugin#8 | e88dabd | 873 passed, 1 xfailed; 14 tests cover both append paths (record_profile_change, record_profile_deletions) and compaction; 7 lock mutants each fail 4–14 of them (helper without `FOR UPDATE`: 14, before: 1 of 705); wait probe stable in 40 runs under 6 CPU burners |
| P3-4a collections feed epoch, snapshot, bounded writes | merged | rendyhd/lumae-plugin#8 | e88dabd | 893 passed, 1 xfailed; K8: feed adds `epoch`/`head_seq`/`floor_seq`/`has_more`, 410 `collections_resync_required` only with an echoed `epoch` (old-client transcript byte-identical to the f36e087 golden), `GET /api/collections/snapshot` in one owned REPEATABLE READ read, one at a time per worker (20k items 0.38 s, 8.0 MB / 0.7 MB gzip); events written as one seq block per mutation; 3 s lock bound → 503 `collection_busy` + `Retry-After: 5`; restores in resumable ≤2,000-row chunks (an unfinished restore's key is 409 for any other request; the web manager keeps one key per backup file). 20k restore: feed head held 17.6 s in one hold (other principal's PATCH timed out at 4.0 s) → 11 holds of ≤67 ms, other principal's write max 76 ms; 2,000 collections × 10 items: last-chunk hold 435 ms (review) → 73 ms, write max 98 ms; × 50: write 769 ms (review) → 201 ms; only the event and receipt INSERTs run while the head is held; restore 32.3 → 6.7 s; review round 1: MEDIUM-1/2 and LOW-3/4/5 fixed, snapshot single-flight added; mutations caught: 410 without echo, no lock_timeout, snapshot in READ COMMITTED, block off by one (both ways), per-collection reads under the head, tombstone ignored, key reuse during a restore, no snapshot guard |
| P3-2 K6 edge references | merged | rendyhd/lumae-plugin#8 | e88dabd | 881 passed, 1 xfailed; the journal stores the waveform part plus edge_ref (kept marker for waveform-only republishes), old rows untouched and served as stored; opt-in edge_refs=1 (/changes) and edge_refs:true (v2 create, per session); old-client responses byte-identical to goldens generated from f36e087; per event with the opt-in 806 B decoded / 124 B gzip (300 events at test scale); publication p95 (complete_attempt, scale 1, 3 runs each) 7.43/6.08/7.37 → 3.67/4.56/4.36 ms, within the ≤5 ms budget; e2e 0.05 first_load_v2/first_load_legacy/reanalysis_noop/lum005_k6/kill_restart PASS; lum005_k6 at 94k: 150 B/track with K6 (14 MB), 8,329 B/track without (783 MB), both devices equal to the server; 6 mutations caught |
| P3-10 LUM-021 diagnostics closure | merged | rendyhd/lumae-plugin#8 | e88dabd | 1086 passed, 1 xfailed; stored error text shown through the new `redaction.py` (DSN/URL userinfo, Subsonic t/s/p and secret query params, Bearer/Basic/cookie headers, key=value secrets incl. escaped JSON, JWT/API-key/long tokens, file paths incl. quoted paths with " (" or ": "; whitespace collapsed first; retry codes unchanged; 300-char cap; every pattern linear, timed on 100 KB adversarial inputs: review HIGH-1 `"token."*4000` 17–19 s → 1.2 ms) on `/database-state`, the settings page (relationship, preparation, backfill, reconcile events, action errors), `/api/catalog/health` (`servers[].{catalog,analysis,preparation,provider_identity_transition}.last_error`, discovery `reason`) and `/api/catalog/prepare` `last_error`, field names and types unchanged (contract §2); on `/database-state` every read in a savepoint under `SET LOCAL statement_timeout` (setting `diagnostic_statement_timeout_ms`, default 5000, clamped 1000–30000; slowest read 0.34–0.39 s p50–p95 at scale 1, page ~1 s), rolled back so the host timeout and transaction are unchanged; a failed or timed-out section shows "unavailable", logged by class; waveform counts are published profiles plus 9 work states mirroring `fetch_backfill_rows` (equivalence test on every state) and failure categories from `profile_publication`; every mutation caught (8 diagnostics, 10 redaction sites, 8 redactor incl. the old key=value pattern failing the timing test, 5 review fixes incl. M7) |
| P3-8 LUM-018 execution limits | merged | rendyhd/lumae-plugin#8 | e88dabd | 964 passed, 1 xfailed; each waveform/edge analysis runs in a child killed 30 s after `analysis_time_limit_seconds` (default 900; also the analyzers' soft deadline, passed per file; SIGTERM, SIGKILL after 5 s). Child = a fork of the job when the process has one Python thread (every AudioMuse job), else a pooled exec worker tied to its thread; a worker killed while idle is replaced uncounted. Host-like jobs (fresh fork per job, 4-min FLAC, 4 CPUs, load 3–8): 1-track edge and 3-track waveform jobs fork path −29 to +50 ms per job (≤2%, noise), ~12 ms/file quiet; exec path +1.13–1.53 s per job (+41–46%), worker start 1.1–1.4 s, RSS 124 MB / 250 MB peak. Categories `media_error` (revision) and `analysis_crash` (transient); SQL retry selection takes the Python set; safe diagnostics in `source_profiles.failure_diagnostics` and the edge job reason; song hook covered through a real child on both paths. 30 mutants caught incl. review M6/M14/M20/M21 and fork-path F1/F2b/F3–F9; F2 (raw close of inherited fds in the child) survives and is harmless: the kernel keeps the parent's socket open |
| P3-4b collections conflicts (K9), shelves hardening, small fixes | merged | rendyhd/lumae-plugin#8 | e88dabd | 938 passed, 1 xfailed; K9 behind `X-Lumae-Collections-Contract: 2` (health `collections.contract: 2`): `idempotency_key_conflict` adds `current` (the collection the key's receipt applied to, now recorded with each receipt; `null` for restores and pre-1.3.0 receipts), a duplicate membership on item PUT or batch upsert is 409 `membership_conflict` `{item_id, existing_item_id, conflicts, current}` and writes nothing, a create with an existing or deleted id is 409 `collection_exists` / `collection_deleted` with `current`; restores unchanged in both modes; without the header the 54-request old-client transcript (collections and shelves) is byte-identical to the 8742700 golden; shelves: the rekey takes the scope lock before `nextval` (late-commit test on `second_connection`), receipts store a body fingerprint, a mismatch is 409 with the header; unknown host auth methods get 401 instead of a default principal on the collection, shelf and discovery routes, while health keeps 200 with `scope: null` (a malformed session keeps its 1.2.5 401); dot-only library item ids refused; album label reads `provider_catalog`; `unaccent` created when permitted, otherwise a warning and accent-sensitive search; dead name-guessing helpers removed; P3-4a follow-ups: tombstone of a collection finished in an earlier chunk (M21 caught), UI restore key kept per backup checksum; 25 mutants caught, including health aborting on an unknown auth method, silent remap under the header, `current` missing, shelves `nextval` before the lock, unknown auth mapped to a default principal, dot-only id accepted |
| P3-5a workbench catalogue scoping (K10, LUM-013); LUM-015 newest-year sort disabled | merged | rendyhd/lumae-plugin#8 | e88dabd | focused files pass (collections, workbench, migration locks, unit); library reads take `catalog_instance_id` (one active source: default, byte-identical to the pre-K10 golden plus an echo; several: 400 `catalog_instance_required`); stream/art provider from the source row; items get nullable `catalog_instance_id` (unique indexes on `COALESCE(…, '')`, backfill only with one source ever); health `collections.source_scoped_items` |
| P3-5c workbench LUM-016 search and paging: stored `search_text`, trigram index, keyset paging, capped totals | merged | rendyhd/lumae-plugin#8 | e88dabd | Review CHANGES_REQUIRED (cursor not bound to its section; OFFSET sorts returned no cursor, so the workbench could not load more), fixed in 7a5d217: cursors carry section and order, OFFSET sorts carry the next offset; 4 mutants killed. Focused runs only (full suite left to the orchestrator). Scale 1 (132k tracks, `scripts/perf/wb_bench.py`, median of 5), before → after: browse albums 222 → 14 ms, tracks 407 → 1.6 ms, artists 332 → 41 ms; search `all` "artist 12" 2854 → 105 ms, "e5d0" 1567 → 9 ms, "love" 1508 → 2 ms; tracks "café song" 1224 → 3 ms; deep page 1000 by cursor 665 → 1.6 ms (legacy `page=1000` 225 ms). Upgrade migration on that fixture: 15.5 s once (backfill plus 4 btree and 1 GIN index), then 1.1 s, a no-op. `catalog_tracks.search_text` = `lower(unaccent(concat_ws(title, artist, album artist, album name)))` is written with the rows at publication, and the migration rewrites stale generations. `catalog_state.search_text_generation`/`search_text_folded` gate its use, so otherwise search computes the text inline as before. The trigram GIN is created when `pg_trgm` can be installed: otherwise a warning, and search gives the same results. Keyset indexes are on `(catalogue, generation, lower(key), id)`, plus `(catalogue, generation, album_id)`. Totals are exact up to 1000, then `1000+` (`total_exact:false`); unfiltered tracks use `entity_counts.track`. `page` is legacy offset paging. Results equal the pre-LUM-016 queries (oracle test, accent and case variants, stored and inline). EXPLAIN checks: trigram bitmap scan, and a keyset index scan scoped by catalogue and generation with no Sort and no OFFSET. The P3-5a golden passes, with only `next_cursor`/`total_exact` stripped after asserting them. 3 mutants (OFFSET on cursor pages, no search text at publication, keyset index without generation) are each killed. Contract §5.1b; hand-off C-13 server note. |
| P3-5b workbench LUM-014 albums by catalogue `album_id`; per-item catalogue for art/stream; K10-scoped rekeys | merged | rendyhd/lumae-plugin#8 | e88dabd | Focused runs only (full suite left to the orchestrator): collections, workbench, library integration, rekey, provider-identity and shelves files, plus the collection/library/album/rekey/identity subset of `test_lumae_analysis.py`, pass. Browse, album search, stats and the artists album count go by `catalog_albums.album_id` in the current generation; every album row carries `provider_album_id`; same-name editions are separate (the editions probe inverted into a test). Details are `(catalog_instance_id, provider_album_id)`, with title and artist taken from the catalogue; title and artist alone are the documented legacy `album_key` fallback (one edition, lowest `album_id`). The P3-5a golden passes, with only the browse rows' `provider_album_id` value undone after asserting it. The workbench passes each collection item's `catalog_instance_id` to art, stream and album details (`catalogOf`), falling back to the page catalogue for `null`; a mixed-catalogue test covers it. `rekey_collection_items` takes the rekeyed catalogue and touches only its items and `null` ones; the merge groups by catalogue like the unique indexes, and rekey upserts carry `catalog_instance_id`. 3 mutants (browse by `album_key`, art from the global catalogue, unscoped rekey) are each killed. Release is gated on the client C-13 album-index change (contract §5.1a, hand-off C-13 change 5). |
| P3-7 LUM-008 gaps: orphan withdrawal, backfill source, ready repair | merged | rendyhd/lumae-plugin#8 | e88dabd | 934 passed, 1 xfailed; orphan pass (published profiles whose track the current generation lacks; a diff never names a track missing from both generations) runs after every catalogue refresh, with or without changes, after the commit, and in install maintenance (after the 1.2.5 seed): catalog_state lock and re-check per batch of 1,000, stale attempt, `record_profile_deletions`, edge sweep; a generation without rows or an inactive source withdraws nothing; the real 1.2.5 upgrade test withdraws the seeded orphans (delete last, attempt stale, edge swept) and a re-install changes nothing; scale 1 (94k profiles, 132k tracks): 20,000 orphans in 1.9 s, 20 holds of max 69 ms (median 51 ms), 0.13–0.21 s read outside the lock when there is nothing to do; `edge_backfill_candidates` reads `published_source_profiles`; repair D (settings action, and install) republishes current ready-unpublished rows with `complete_attempt`'s checks and `record_profile_change` (25 per hold, 2,000 per run): 5,000 rows 12 s, holds max 141 ms (median 57 ms), stale-media rows never republished; `profiles_unpublished_ready` count planned as a cross join on the source (1,000 rows 20.6 s, 5,000 > 120 s) → per-row key lookup (5,000 rows 0.17 s); health `integrity.profiles_orphaned`; 11 mutations caught |
| P3-4c collections rekey through the protocol, ANALYZE after generation copy | merged | rendyhd/lumae-plugin#8 | e88dabd | Focused runs only (full suite left to the orchestrator): 130 collections/shelves tests and 54 rekey, fence and status tests pass. Collection items rekey under their parents' row locks, taken in sorted order (`collection_manager.rekey_collection_items`), late in the publication. When the new id is already in the collection, the rekeyed item is deleted with an item delete event. Rewritten items get upsert events, and each live collection's revision is bumped once. The events are recorded with `_record_changes` (the P3-4a block) just before commit. Delivered `collection_changes` rows and `collection_mutations` receipts are no longer rewritten. When the merge can't resolve a principal's collision, that principal is deferred (nothing written) and a diagnostic goes to the new `provider_identity_transitions.collection_deferrals`; the others proceed. `_copy_analysis_generation` runs `catalog_analysis._analyze_generation_keys` (analysis_items, then links) before commit. The K9 header test adds `2.0` and `02`, and a new test pins that a shelf 409 is never stored or replayed. 3 mutants (history rewritten, no delete event on a merge, no ANALYZE) are each killed; 2 review-gap mutants are killed too. With 20k items across 20 collections, staging takes 1.1 s and staging plus the event insert 1.7 s. K10 catalogue scoping is left to P3-5 (TODO). Review round 1: a restore chunk locks all its existing collections in one sorted statement, in the same order the rekey uses, and a deadlock test covers it. The rekey finds and locks the parents in one statement. A new test pins tombstones. Deferrals are appended with their `transition_id`, and runbook repair E says how to resolve them. A later rekey does not retry them. 5 mutants are each killed. |
| P3-6 LUM-007 retry gaps, shared scheduler predicates | merged | rendyhd/lumae-plugin#8 | e88dabd | focused: 236 passed (retry, retry gaps, publication, repair, database_state, isolation, status summary, upgrade fences) + 48 passed (`test_lumae_analysis.py -k backfill/release/pause/retry/recover/rekey`); every stale transition (completion with changed/missing media, epoch rebase, catalogue withdrawal, orphan withdrawal, provider rekey) clears `retry_category` and `failure_diagnostics` and keeps `retry_count`, so a retried failure abandoned by it is selected again when the media returns (probe "stranded retry" inverted for four transitions); pause (3 sites), legacy-job migration and batch abort release with `count_failure=False` with no cooldown (`retry_after=now()`: due as soon as the pause ends, also after 2 used attempts; probe "pause burns attempts" inverted: 4 releases, count 0, still due); counted releases, recovered claims and failures share `retry_delay_sql`: 60/300/1800 s by attempts used; `migrate` clears the category and diagnostics of rows stranded before 1.3.0 once (marker `stale_retry_category_v1`, read first so a re-run takes no lock on `source_profiles`; released rows untouched); provider-rekey reset tested; `release_attempts` and `recover_stale_pending_profiles` lock in `track_id COLLATE "C"` order (admission-first/release-first test, the pre-P3-6 dict-order loop deadlocks); `backfill_due_sql`/`RETRY_ARMED_SQL`/`scheduler_params` in `profile_publication` used by `fetch_backfill_rows`, `next_profile_retry_at` and `/database-state` (same rows as the frozen pre-P3-6 SQL on the P3-10 fixture); P3-7 gaps: orphan by `available=false`, concurrent publication between candidate read and batch; mutants caught: pause counts, uncounted release cools down, no one-time cleanup, rekey keeps category, dict-order locking, stale keeps category (all / completion only), no published-row check; survives: dropping only the `ORDER BY` (the plan locks in key order anyway); review round 1 (2 MEDIUM, 1 LOW): a stale row without a category is due only while `retry_count < RETRY_LIMIT` (an exhausted row stays exhausted through a stale transition or the one-time cleanup, which now skips exhausted rows; new media is still due; the only deliberate difference from the pre-P3-6 SQL, pinned in the equivalence test; diagnostics count it `exhausted`), a batch abort sets the backfill `next_retry_at` 60 s ahead and that delay wins over the wake its own admissions arm (it was reset to now, so a persistent error re-aborted every reconcile tick), recovery/admission lock-order test; 4 more mutants caught |
| P3-9 LUM-017 readiness UI (plugin side) | merged | rendyhd/lumae-plugin#8 | e88dabd | focused: 78 passed (`tests/plugins -k "settings or status or database_state"`), incl. 5 new render/a11y-lint tests and 1 new Postgres zero-write test; new `render_readiness_streams_panel` shows catalogue, analysis projection, waveform profiles, edge profiles and relationships as five labelled `<article>` regions per source, each with an availability word (ready/partial/unavailable, never colour alone), a last-success time and age, and a job status (idle/queued/running/failed/cooling until `next_retry_at`); reuses P2-1's `status_model`/committed counts, the existing `preparation_state`/`profile_backfill_state`/`relationship_status` readers and P3-10's `redact_stored_error`; the only new read is `edge_profile_store.edge_profile_status`, one small source-scoped aggregate (PK-prefixed, no new heavy query); scoped retries reuse `prepare_lumae` (catalogue + projection) and `start_backfill` (waveform) unchanged, plus two minimal new actions, `retry_edge_profiles` and `retry_relationships`, that wire existing enqueue functions since those two streams had no settings-page retry path; one `aria-live="polite"` summary line per panel (deduped by the existing settings-poll snapshot diff, so it only announces on an actual change); a hand-written `html.parser` a11y lint (button names, live region, labelled regions, no onclick-only divs, no inline px container widths) is clean on all 4 fixture states (all ready, one failed with a redacted secret, one cooling, one unavailable); both required mutants (dropped live region, unredacted error) are caught in a self-check test; GET `/settings` and `/settings/status` proven to write nothing against a real seeded Postgres fixture (P2-1's `RecordingConnection`: 0 commits, 0 writes). Open question: catalogue and analysis-projection share one retry action (`prepare_lumae`) since the plugin only tracks one combined preparation job for both phases — flagged for reviewer sign-off rather than adding a second, narrower action. |
| P3-12 LUM-019 documentation | merged | rendyhd/lumae-plugin#8 | e88dabd | docs only; root `README.md` rewritten to describe current (1.3.0-unreleased) capabilities — catalogue, waveform/edge profiles, offline bulk copy (v2 bootstrap, K6 edge references), collections (K8 epoch/snapshot, K9 conflicts, K10 catalogue scoping), diagnostics/readiness panels, analysis time limits — with a Settings table (`analysis_time_limit_seconds`, `diagnostic_statement_timeout_ms`, `edge_profiles_enabled`, `collection_manager_enabled`) and a link to `docs/runbooks/UPGRADE_1.3.md`; DJ-era and other per-release narrative moved to new `CHANGELOG.md` (Unreleased (1.3.0) summary plus 1.0.0–1.2.5 history); `runtime/README.md` marked historical with a banner; `AUTH_ENABLED=false` anonymous-transfer behaviour documented, linking `docs/contracts/LUMAE_SYNC_CONTRACT.md` §1.2 rather than duplicating it; `python scripts/build_catalog.py --check` still passes; all relative links verified to resolve |
| P3-11 LUM-020 slice 1 — settings_render.py extracted; task dotted-path guard | merged | rendyhd/lumae-plugin#8 | e88dabd | orchestrator-verified: all 22 moved definitions byte-identical apart from `_pkg.` qualification, no unresolved globals; fix e990c07 binds the package via `sys.modules[__package__]` (the first cut imported `plugins.LumaeAnalysis`, a name that exists only in the test layout), with two guard tests that fail without it; remaining LUM-020 slices (`preparation.py`/`profile_backfill.py`/`analysis_runs.py` with task shims, route modules, `schema.py`) deferred to after 1.3.0; behaviour-preserving move only: `plugins/LumaeAnalysis/settings_render.py` now holds the settings-page panel renderers and their private helpers (route handlers and `register` stay in `__init__.py`, which re-exports every moved name); a new guard test, `tests/plugins/test_task_paths.py`, calls `register` with a recording fake ctx and pins the dotted path of every task/cron/hook callback so a later move that breaks a persisted task-queue or cron path fails loudly; focused suite (`test_task_paths.py` + `test_lumae_analysis.py`) 291 passed both before (9a68eef) and after the extraction |
| P3-1 LUM-005 loudness analyzer v2 (K11) | blocked on U3 | — | — | Needs the user's 200–500-track real-library sample for the v1→v2 mapping spike (P3-1a). Regeneration must also wait until clients adopt K6 edge references (C-10). Not started. |
| P3-3 K7 compact edge transport (optional) | deferred | — | — | The plan ships it only when client C-12 is ready. The Auralscape side has not started it, so it is closed as deferred; the stored payload and digest are unchanged, so it can be added later behind `capabilities.edge_profiles.compact_transport`. |
| F1 multichannel decode crash (SIGSEGV) | on phase/3f-followups | — | — | Root cause: PyAV 16 `AudioFrame.planes` walks `extended_data` to a NULL that an 8-channel planar frame lacks, so `to_ndarray()` read a bogus 9th plane (7.1/8-ch SIGSEGV; 1–7 ch fine); loudness now decodes layouts wider than stereo to packed `flt` and de-interleaves, mono/stereo keep `fltp`; 7.1 analyzes, results for 1–7 ch byte-identical (96 codec/layout files old vs new); edge already refuses >2 ch cleanly; focused: 377 passed, 66 skipped (PG absent) |
