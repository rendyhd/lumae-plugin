> **Historical record.** Current work status lives in [docs/STATUS.md](../STATUS.md); the active plan is [docs/plan/LUMAE_FINAL_PLAN_2026-09-24.md](../plan/LUMAE_FINAL_PLAN_2026-09-24.md).

# Lumae remediation ledger

Updated: 2026-09-24. Owner: remediation lead (current parent model/effort unverified). The user-supplied V2 resume brief supersedes routing and startup in the historical [master orchestration](LUMAE_ASTRA_MASTER_ORCHESTRATION.md); existing findings, decisions and evidence remain. No deployment, push, production data access or device mutation is authorized by this program.

## Current result: LUM-010 source-scoped stock-host qualification (2026-09-24)

- **Decision: NO AUDIO MUSE PR REQUIRED for LUM-010.** The earlier immutable-account/authorization-generation/host-owned-connection design was reviewed exploratory work, based on treating shared source-derived profile data as personally owned. Source revalidation and two unmodified stock-host runs overturned that premise. The former host SHAs `3d6b40c8d8417e6907ca8dfc645ea19fd2a552ca` and `89f2c9d43f0d9eaedfdf87bfc09e50299935d2bd` remain historical and are not to be upstreamed solely for Lumae. The historical sections below are retained unchanged as dated evidence.
- **`LUM010_NO_HOST_PR_PLUGIN_SHA` = `48545bdab07c7ebe80d04fc38333d5c6390d750d`**, local-only branch `codex/lum010-no-host-pr` from `5612f4d988a386b9f8d19cafc065958e83d87069`. **`LUM010_NO_HOST_PR_CLIENT_SHA` = `4416a6172761d29cee359596aeb3a60ea3312606`**, isolated Auralscape branch `codex/lum010-no-host-pr` from `de4ad73f8c674335f4cb23628af1b4cfd8409cf9`. No prior checkpoint was amended. This ledger/RESUME update is a separate bookkeeping commit; resolve that commit's own SHA after creating it.
- [Executed stock-host qualification and limits](evidence/lum010_no_host_qualification/QUALIFICATION.md): original unmodified AudioMuse `f100684b1753e303f2b698b4f56f5b1b8972a5bb` and recorded upstream main `ce742938e5ad86be85b978effa1375c4e3e9e633`, each with real host HTTP auth, plugin loader, disposable PG17/Redis, bearer/account transfer, process restart, source isolation, finite catch-up, later delta, owned-backend/request-transaction separation, credential rejection/rotation, actual client SQLite resume/publication/crash matrix and authoritative empty withdrawal. Both stock tracked trees were clean.
- Lumae focused PostgreSQL v2 **22 passed**; full serial plugin suite **569 passed / 3 unchanged published 1.2.5 archive identity failures**. Auralscape full serial Jest **913 suites passed / 2 skipped; 8,908 tests passed / 6 skipped**. TypeScript, changed-file functional ESLint, Python compilation and staged whitespace checks passed. The earlier performance-continuity SQL extraction defect was corrected. Full ESLint's Prettier rule still reports formatting debt in changed existing client files; functional rules pass. Fresh independent configured Sol High read-only review returned **PASS** with no production finding; runtime-resolved model identity was not observable.
- Lumae migration deletes only incompatible experimental account-bound v2 sessions, keeping published, legacy and personal data. Auralscape schema 37 drops unfinished v36 v2 staging/resume and marks full refresh, keeping published/personal state. New health capability names `source_scoped_v1` and existing host authentication; token/source/epoch validation remains. Plugin-owned `psycopg2.connect(plugin.api.config.DATABASE_URL)` supplies a separate bounded backend. Native Windows stock host with Docker PG/Redis was executed; AudioMuse application-container/custom-role topology was not.
- No push, deployment, tag, release, production data or published archive rebuild. Remaining gates are populated-state cutover/old-worker drain, native mobile cookie/provider admission and device behavior, Docker-host/custom database topology, restore/clone continuity characterization, performance/audio and release packaging. None reinstates an AudioMuse core PR requirement based on the executed protocol evidence.

## Current active task: LUM-010 qualification checkpoint (2026-09-24)

- Qualification-only branch `codex/lum010-qualification-20260924` began at plugin bookkeeping SHA `dbf9297e55490f05406e5ac6b056d10a2a091c92`. Frozen host `89f2c9d43f0d9eaedfdf87bfc09e50299935d2bd`, plugin implementation `4b547927c60ec5aad9f728fb6b67d46c836233a8`, and Auralscape `de4ad73f8c674335f4cb23628af1b4cfd8409cf9` were not changed. This checkpoint adds only disposable qualification harness/evidence and bookkeeping. The shared Lumae checkout's pre-existing `.codex/config.toml` edit was not touched.
- [Detailed qualification evidence](evidence/lum010_qualification/QUALIFICATION.md): dedicated PG17/Redis, real AudioMuse host and Lumae plugin, account login/create/page/replay/catchup/release across verified host process restart, frozen Auralscape `AudioMuseClient` plus durable SQLite bootstrap/publication **2 passed**, and same-cluster disposable dump/restore clone with a second host and independent account-session replay. The client integration uses a Node cookie shim and a no-op LUM-009 admission guard; native cookie/provider admission is not qualified. The clone used the same synthetic JWT secret; cross-cluster restore is not qualified.
- Rebuilt disposable PG17 full plugin suite: **567 passed, 3 failed**, exit 1; exactly the unchanged published 1.2.5 archive/source identity gates. The first 512 MB disposable tmpfs filled during a run; it was recreated at 4 GB and all stated integration and plugin results were rerun against the new verified cluster. Frozen full Auralscape Jest: **912 passed, 1 failed, 1 skipped suites; 8,905 passed, 1 failed, 3 skipped tests**, exit 1. The single failure is the existing `scripts/check-performance-continuity.cjs` SQL extraction leaving new schema template placeholders unexpanded (`unrecognized token: "$"`). Neither frozen client source nor the published archive was changed.
- A separate configured `gpt-6-sol` High reviewer returned **PASS** on the bounded qualification after evidence corrections; resolved model identity was not observable. All seven Python harness modules compiled and the optimized-Python preflight rejected forged work-directory and PostgreSQL-cluster inputs. Both verified disposable host processes were stopped and only the `lum010qual20260924` Compose project was taken down. Synthetic credentials and raw logs remain private outside Git; its PostgreSQL tmpfs data is gone.
- **LUM-010 remains IMPLEMENTED_UNVERIFIED for release.** Old-worker drain, real pre-remediation populated-state cutover/rollback, live legacy collection-cursor reconciliation, live LUM-009 admission, native/device cookie behavior, cross-cluster restore/secret rotation, full host suite, mixed-version/audio and release gates remain OPEN. The synthetic fixture inserted published profiles/events directly, so this integration did not exercise actual media analysis or production-load compaction. No push, tag, deployment, production change, archive rebuild or release-version selection occurred. Stop after this bounded qualification.

## LUM-010 client checkpoint (2026-09-24)

- **LUM010_CLIENT_CHECKPOINT_SHA:** `de4ad73f8c674335f4cb23628af1b4cfd8409cf9`, local-only Auralscape branch `codex/lum010-client-resume-20260924`, based exactly on LUM-009 `9fa3f572cc3d97df4dbd8f8369e593e43ea4a432`. The isolated client worktree was clean after commit. LUM-008 and LUM-009 checkpoints were not amended.
- Independent Sol High review identified a P1 shared-cookie account race and a P2 local-clock rejection of a server-accepted final replay. A bounded cross-repository protocol correction was necessary: AudioMuse-AI AJAX login now exposes the issued account subject/authorization-generation binding in local-only commit `89f2c9d43f0d9eaedfdf87bfc09e50299935d2bd`; the plugin echoes its validated v2 create binding and advertises exact health capability in local-only commit `4b547927c60ec5aad9f728fb6b67d46c836233a8`. The client compares bindings before staging, persists the binding for resume, reauthenticates before replacement create, and uses server validation after final replay. A further P2 mixed-rollout delta-410 fallback finding was corrected; independent targeted re-review returned **PASS**. Configured reviewer was GPT-6 Sol High; runtime-resolved identity was not observable.
- Auralscape schema v36 adds durable source/account/principal-bound session state. Snapshot rows or catch-up tombstones and page progress commit atomically in existing LUM-008 staging; the LUM-008 publication transaction promotes the authoritative generation and cursor and marks release pending. The frozen historical v35 fixture upgrades without resetting published or personal state. Password/account mode requires exact v2 capability and matching login/create binding; bearer and old plugins stay legacy, and a mixed new-plugin/old-host login falls back before v2 staging.
- Frozen-source full serial client Jest: **8,879 passed, 27 failed, 3 skipped** across 909 passed, 4 failed, 1 skipped suites. All 27 failures are the four pre-existing sandbox `spawnSync EPERM` suites; no new non-environment failure remains. TypeScript, changed-file functional ESLint, staged diff check and independent review passed. Host auth unit tests: **94 passed**. Plugin health selection: **20 passed**; the PostgreSQL-backed create-echo test skipped without a disposable database. Earlier server evidence remains **566 passed / 3 unchanged published 1.2.5 archive identity failures** at the host-adapter checkpoint. These are separate evidence states, not release qualification.
- Client-to-real-host integration remains **OPEN** without verified disposable `LUMAE_POSTGRES_TEST_DSN` and `LUMAE_HOST_SOURCE`. Host/plugin merge and rollout, old-worker drain, populated-state cutover and rollback, restore/clone continuity, bearer/installation-principal v2, native/device qualification, four sandbox-blocked Jest suites, and release candidate/archive qualification remain open. No push, tag, deployment, production data change, archive rebuild or version selection occurred. Stop after LUM-010 client; performance work is not authorized by this task.

## Baseline

| Repository | Starting SHA | Branch | Initial worktree |
|---|---|---|---|
| lumae-plugin | 4276398c671f3e2713212488f4e72a32553697d7 | main | Clean |
| Auralscape | 6501c83da0691e42c304961d580ece22debd76cb | main | Dirty: pre-existing navigation, player, settings and related test changes; preserve all |
| AudioMuse-AI (read-only host evidence) | f100684b1753e303f2b698b4f56f5b1b8972a5bb | Not modified | Pre-existing untracked deployment/test/docs files |

At the initial baseline, the plugin matched the reviewed SHA. Auralscape differs from the reviewed 82f4917ef7d55971ff6a6e784b57bb076373f10b. Audit claims require current-source adjudication. A passing dirty-worktree suite does not qualify an exact clean candidate.

## Local server remediation checkpoint (2026-09-23)

- **SERVER_REMEDIATION_CHECKPOINT_SHA:** `816420f944bdac02c620928e46d752fde981d577`. Commit `remediation: checkpoint server integrity and profile lifecycle` is the immutable local implementation baseline for the completed server integrity/profile-lifecycle wave. It was created locally only and has **not been pushed**. It is **not a release-qualified SHA**.
- Findings in that commit: **LUM-001** profile journal allocation; **LUM-002** concurrent collection revision; **LUM-003** receipt/mutation atomicity; **LUM-004** committed feed frontier; **LUM-006** backfill eligibility; **LUM-007** repaired-media retry; **server-side LUM-008** publication/attempt lifecycle; and **plugin-side Task 0B / LUM-021** safe diagnostics. The associated schema migrations, regression tests, implementation/design notes and scoped test evidence are included.
- The exact implementation source state had a final serial disposable PostgreSQL 17 plugin run of **546 passed, 3 failed**, exit 1. The three failures are the unchanged published 1.2.5 archive/source identity checks in `tests/plugins/test_release_channels.py`; they are **open release gates**, not passing tests. The published archive was not rebuilt. Before the commit, affected Python compile/py_compile checks, unstaged `git diff --check` and staged `git diff --cached --check` passed. The full suite was not rerun solely for the checkpoint because the implementation source state was unchanged.
- Remaining gates: real-host integration (including admission, cancellation, timeout and worker behavior); draining old writers/workers and reconciling legacy cursors; populated-state migration/cutover and rollback compatibility; **historical Auralscape client profile convergence** for LUM-008; LUM-010 durable versioned producer/consumer resume design and implementation; and candidate archive/build, broader integration, migration, audio/protocol and release qualification. None of the findings in this checkpoint is thereby marked VERIFIED.
- **LUM010_START_SHA:** `95f56298e4d3c91230c66c1fcffdb75f05c2afb5`. This is the separate local bookkeeping commit `docs: record remediation checkpoint`, containing the server implementation plus the checkpoint documentation. It is the LUM-010 orchestration/resume baseline and was not pushed. The post-commit worktree was clean. This exact SHA is recorded at the first normal LUM-010 documentation update, without amending either checkpoint commit.

## Findings

Adjudication (PRESENT, PARTIALLY_FIXED, ALREADY_FIXED, NOT_REPRODUCED, BLOCKED_BY_ENVIRONMENT) is recorded separately in evidence; workflow status below is not a substitute for it. INVESTIGATING rows are not yet adjudicated.

| ID | Finding | Repo | Priority | Status | Owner | Dependency | Implementation SHA | Verification | Remaining gate |
|---|---|---|---|---|---|---|---|---|---|
| LUM-001 | Profile journal allocation race | plugin | P1 | IMPLEMENTED_UNVERIFIED | Terra audit; Sol worker/reviewer requested | 0A; accepted design | 816420f944bdac02c620928e46d752fde981d577 | 26 PG17 cases; affected 93 pass; independent scoped PASS; full 464 pass/3 archive failures | Existing-state consistency, old-worker drain, host/release gates |
| LUM-002 | Collection concurrent revision overwrite | plugin | P1 | IMPLEMENTED_UNVERIFIED | Terra audit; Sol worker/reviewer requested | 0A; transaction design | 816420f944bdac02c620928e46d752fde981d577 | 26 PG17 cases; independent scoped PASS; full 490 pass/3 archive failures | Old-worker drain; host, LUM-004 and release gates |
| LUM-003 | Receipt/mutation atomicity | plugin | P1 | IMPLEMENTED_UNVERIFIED | Terra audit; Sol worker/reviewer requested | 002 joint transaction owner | 816420f944bdac02c620928e46d752fde981d577 | 26 PG17 cases; independent scoped PASS; full 490 pass/3 archive failures | Old-worker drain; host, LUM-004 and release gates |
| LUM-004 | Late-committing feed event | plugin | P1 | IMPLEMENTED_UNVERIFIED | Terra audit; Astra design/review, Sol worker requested | 002/003 transaction owner | 816420f944bdac02c620928e46d752fde981d577 | 5 feed PG17 cases; affected 54 pass; independent P1 PASS; full 495 pass/3 archive failures | Old-worker drain, legacy cursor reconciliation, host/migration and release gates |
| LUM-005 | Unqualified loudness semantics | both | P1 | CONFIRMED | Terra audit / Sol design | Qualified versioned contract | — | Source confirmed; see Phase 0 reports | Reference audio; producer/consumer adoption |
| LUM-006 | Backfill eligibility starvation | plugin | P1 | IMPLEMENTED_UNVERIFIED | Terra audit; Sol worker/reviewer requested | 0A | 816420f944bdac02c620928e46d752fde981d577 | PG17 LIMIT-1 red/green; focused 23 pass; independent P1 PASS; full 503 pass/3 archive failures | Host/existing-state and release gates; LUM-007 lifecycle |
| LUM-007 | Repaired-media recovery | plugin | P2 | IMPLEMENTED_UNVERIFIED | Terra audit; Sol lead and independent reviewer | 006; LUM-008 token/publication foundation | 816420f944bdac02c620928e46d752fde981d577 | 28 new retry cases (27 real PG17, one bounded unit); independent Sol High PASS; full plugin 546 pass/3 archive failures | Host/old-worker and release qualification |
| LUM-008 | Published validity vs job state | both | P2 | IMPLEMENTED_UNVERIFIED | Terra audit; Astra design; Sol lead and independent reviewer | 001; lifecycle design | Server routing 816420f944bdac02c620928e46d752fde981d577; client included by LUM-009 baseline 9fa3f572cc3d97df4dbd8f8369e593e43ea4a432 | 13 PG17 publication, 2 migration, 26 stream cases; client LUM-008 authoritative convergence and atomic publication reviewed; full plugin 518 pass/3 archive gates | Host/old-worker and populated-state cutover, client real-host convergence and release gates |
| LUM-009 | Late provider preflight | client | P1 | IMPLEMENTED_UNVERIFIED | Terra audit; Sol implementation and independent Sol High review | 0A; 021; LUM-008 source ownership | Client 9fa3f572cc3d97df4dbd8f8369e593e43ea4a432 | 16 focused suites/260 passed; frozen-source full Jest 8,841 passed/27 known EPERM failures; independent Sol High PASS | Real-host/account transition, native/device and release qualification |
| LUM-010 | Durable profile bootstrap resume | both | P1 | IMPLEMENTED_UNVERIFIED | Terra audit; Astra Medium server/host design; Sol Medium implementation; independent Astra Medium and Sol High reviews | 001/008; versioned producer, host principal and client consumer | Server a713e33f6550f769256f85e06f186298c8875f28; adapter 9914d8562c5f12d77f44a67165824588041e0fc1; host binding 89f2c9d43f0d9eaedfdf87bfc09e50299935d2bd; plugin binding 4b547927c60ec5aad9f728fb6b67d46c836233a8; client de4ad73f8c674335f4cb23628af1b4cfd8409cf9 | Server 566 passed/3 archive gates; host auth 94 passed; client full Jest 8,879 passed/27 known EPERM failures/3 skipped; TypeScript and functional lint passed; independent Sol High PASS | Real client-to-host integration, host/plugin merge and rollout, old-worker/populated-state cutover, restore/clone, bearer v2, native/device and release gates |
| LUM-011 | Expensive admission health | plugin | P2 | CONFIRMED | Terra / Astra | 021; comparable baseline | — | Source confirmed; see Phase 0 reports | Freshness/safety and timings |
| LUM-012 | Projection memory/write cost | plugin | P2 | CONFIRMED | Terra / Astra | Integrity fixes; comparable baseline | — | Source confirmed; see Phase 0 reports | No-change/delta/rebuild benchmark |
| LUM-013 | Implicit catalogue selection | plugin | P2 | CONFIRMED | Terra / Astra | Source contract | — | Source confirmed; see Phase 0 reports | Browse/detail/save/playback scope |
| LUM-014 | Provider album identity loss | plugin | P2 | CONFIRMED | Terra / Astra | 013 | — | Source confirmed; see Phase 0 reports | Same-name editions; no ambiguous migration guesses |
| LUM-015 | Year sort with NULL year | plugin | P2 | CONFIRMED | Terra / Astra | 013/014 | — | Source confirmed; see Phase 0 reports | Authoritative year semantics or disable |
| LUM-016 | Search/count/OFFSET scale | plugin | P2 | CONFIRMED | Terra / Astra | Identity semantics; same-fixture benchmark | — | Source confirmed; see Phase 0 reports | PostgreSQL EXPLAIN ANALYZE at 100k |
| LUM-017 | Readiness conflates enrichment | both | P2 | CONFIRMED | Terra / Astra | Stream outcomes and recovery | — | Source confirmed; see Phase 0 reports | Partial success; polling/focus preservation |
| LUM-018 | Ineffective timeout arguments | plugin/host | P2 | CONFIRMED | Terra / Astra | Verified host boundary | — | Host source enqueue uses job_timeout=-1 | Cancellation, revocation, worker death, decoder stall |
| LUM-019 | Retired Radio DJ guidance | plugin | P2 | CONFIRMED | Terra / Astra | Release capability inventory | — | Source confirmed; see Phase 0 reports | Current guidance vs historical changelog |
| LUM-020 | Fragmented transaction/state ownership | plugin | P2 | CONFIRMED | Terra / Astra | Behavioral fixes last | — | Source confirmed; see Phase 0 reports | Task paths, migrations, endpoints preserved |
| LUM-021 | Incomplete safe diagnostics | both | P2 | IMPLEMENTING | Terra / Astra | 0A | Plugin Task 0B: 816420f944bdac02c620928e46d752fde981d577; client open | Source confirmed; see Phase 0 reports | Safe operation/error/outcome/timing evidence |

## Historical host-adapter task (superseded by the current qualification checkpoint)

- **LUM-010 host adapter checkpoint complete locally (2026-09-23).** `LUM010_HOST_ADAPTER_CHECKPOINT_SHA` is `9914d8562c5f12d77f44a67165824588041e0fc1` (`remediation: adopt qualified host principal and DB lease`). It is a local-only implementation checkpoint, not a host deployment or release-qualified SHA; it was not pushed. The commit contains account-only public `plugin.api.get_principal()` and `open_db_connection()` adoption, provisional v2 session invalidation, regression tests, [design](LUM-010_DESIGN.md), and [disposable real-host evidence](evidence/lum010_host_adapter_integration.py). Legacy routes remain importable on old hosts; their v2 route fails 503. Bearer-only v2 remains 401. No host or Auralscape edit was made in this task.
- Focused plugin PostgreSQL 17: **20 passed**; affected selection: **133 passed, 241 deselected** before the last two lease-error tests; final full serial plugin suite: **566 passed, 3 unchanged published 1.2.5 archive identity failures**, exit 1, 56.11 seconds. Final isolated host+Lumae harness at reviewed host SHA `3d6b40c8d8417e6907ca8dfc645ea19fd2a552ca` passed actual account middleware, four v2 routes, finite tombstone catch-up, replay/restart, password generation and account recreation isolation, bearer 401, role change, request transaction and separate owned backend/schema/role. Harness preflight and cleanup were corrected after independent Sol High CHANGES_REQUIRED; narrow re-review returned **PASS**. Targeted Python compilation and staged/unstaged diff checks passed. The historical host full suite remains incomplete; prior 98 focused and 268 affected passes are separate from plugin results. No archive was rebuilt and no release claim is made.
- **Current LUM-010 state:** server adapter locally implemented and independently reviewed, with disposable host integration evidence. Next safe work requires host API integration/merge qualification, old-worker drain and populated-state migration/cutover, restore/clone continuity, bearer/client adoption and LUM-008 historical convergence, then broader host/client/release checks. The separate commit containing this bookkeeping update records the adapter checkpoint; its exact SHA is resolved after commit and is not inserted into itself.

- **Previous host contract HOLD, before the reviewed host API existed.** Task 3B began from `LUM010_START_SHA` `95f56298e4d3c91230c66c1fcffdb75f05c2afb5` and is preserved at `LUM010_SERVER_CHECKPOINT_SHA` `a713e33f6550f769256f85e06f186298c8875f28`. The separate bookkeeping commit `LUM010_HOST_START_SHA` is `a62f72e2d8595601171ec4e2bf2a197ca735ed44`. Bounded Astra Medium host design and independent Sol High review supported the earlier HOLD on username identity and DSN cloning. The 546-pass / 3 archive identity state is the pre-LUM-010 baseline; the earlier server implementation had a full 558-pass / 3 unchanged archive identity run and a later test-only bearer regression. That historical HOLD was resolved for the account-only adapter by the reviewed host API; deployment and release qualification remain open.

### LUM-010 server implementation checkpoint (2026-09-23)

- **LUM010_SERVER_CHECKPOINT_SHA:** `a713e33f6550f769256f85e06f186298c8875f28` (`remediation: checkpoint resumable profile bootstrap server`). This local-only immutable implementation checkpoint contains the reviewed server code, additive migrations, tests, [design](LUM-010_DESIGN.md) and [compact evidence](evidence/lum010-server-checkpoint.md). It has not been pushed and is **not host-, client-, or release-qualified**. The previous `SERVER_REMEDIATION_CHECKPOINT_SHA` and `LUM010_START_SHA` remain unchanged.
- **The commit containing this bookkeeping update is the next host-qualification orchestration baseline.** Resolve its exact SHA after creating the separate `docs: record LUM-010 server checkpoint` commit. Do not insert that commit's own SHA into itself; record the known value at the next normal host-task ledger/RESUME update.
- `plugins/LumaeAnalysis/profile_bootstrap.py`, `catalog_enrichment.py` and `__init__.py` add opt-in protocol-v2 POST session/create, page, catch-up and release routes, with additive durable session/snapshot/change tables. Legacy profile endpoints remain. Authentication requires `g.auth_user`; tokens stay in private no-store POST bodies; sessions bind principal, source mapping, catalogue/profile epochs, schema, fixed page size and absolute expiry. An owned repeatable-read PostgreSQL capture materializes the committed published/edge baseline and stream cursor from one snapshot in bounded batches, without committing or rolling back host request work. Durable pages replay after process restart. One-time catch-up captures a finite head and interval, including tombstones, then replays independently of later compaction. Limits reject rather than truncate. Design and wire contract: [LUM-010_DESIGN.md](LUM-010_DESIGN.md).
- The independent Astra Medium P1 review first required a session-row lock for page/release race, ASCII-hex signature validation and a finite connection timeout. The Sol Medium writer corrected all three and added regressions. Follow-up independent review: **PASS**, no remaining actionable finding in scoped server diff. Runtime-resolved model/effort is not observable. No nested delegation or concurrent code writers.
- Final focused disposable PostgreSQL 17 four-file selection: **54 passed** (including 13 new LUM-010 cases). Final serial `tests/plugins` on the same disposable PG17, before the last test-only bearer regression: **558 passed, 3 failed**, exit 1, in 68.77 seconds. Failures are exactly `tests/plugins/test_release_channels.py` unchanged published 1.2.5 archive/source identity tests (one current-source comparison and two platform archive comparisons). The archive was not rebuilt; no release version was selected. The earlier sandboxed full-suite attempt failed on pytest temporary-directory ACLs and yielded no tally; an elevated run with a fresh `.pytest-tmp/lum010-full-final-20260923` path resolved that environment issue. `compileall`, targeted `py_compile` and `git diff --check` passed.
- Read-only host source at AudioMuse-AI SHA `f100684b1753e303f2b698b4f56f5b1b8972a5bb` shows `database.get_db()` uses psycopg2, while `app_auth.py` leaves `g.auth_user=None` for bearer-authenticated admin requests. Account-bound v2 correctly returns 401 for those callers; an added route regression proves no database/bootstrap access. The Astra Medium specialist accepted this as a **fail-closed host/client integration gate**, not a reason to weaken identity checks. Future bearer adoption needs a separately reviewed stable principal with account-change, credential-rotation and revocation semantics. This source inspection does not qualify the installed host or a second connection.
- At the server checkpoint, implementation HEAD was `LUM010_SERVER_CHECKPOINT_SHA`; ledger/RESUME bookkeeping was committed separately afterward. No push, tag, deployment, production data action or Auralscape edit. Remaining gates: real-host psycopg2 second-connection credentials/schema/auth and request behavior, including bearer principal support; old-worker drain and populated-state migration/cutover; client durable staging, authoritative source replacement and historical LUM-008 convergence; interrupted client resume/epoch/replay qualification; release archive/build and broader host/client qualification. No finding is marked VERIFIED.

### LUM-010 host contract qualification checkpoint (2026-09-23)

- **LUM010_HOST_START_SHA:** `a62f72e2d8595601171ec4e2bf2a197ca735ed44`, the local-only `docs: record LUM-010 server checkpoint` commit. It was clean at Part B entry. The exact SHA is recorded in this first normal host-task ledger/RESUME update; neither earlier checkpoint was amended or pushed.
- Read-only AudioMuse-AI source `f100684b1753e303f2b698b4f56f5b1b8972a5bb`: public `plugin.api` exports request-scoped `get_db` and `config`, but no immutable principal resolver or owned connection factory. Valid cookie requests select a username and role; a shared bearer alone selects admin authority with `g.auth_user=None`; a valid cookie takes precedence when both credentials are present. Invalid/rotated-old bearer yields API 401 absent a valid cookie. Auth-disabled requests have no account subject. Cookie `iat` and password-change stamp use seconds, so same-second password change or delete/recreate can leave an old cookie valid under a reused username. Existing collection `__global__` bearer sharing does not authorize v2 profile-session sharing. Detailed source evidence and compatibility matrix: [LUM-010_HOST_CONTRACT.md](LUM-010_HOST_CONTRACT.md).
- Public `get_db()` is a Flask-`g`-cached psycopg2 request connection closed at teardown. The private `database.connect_raw()` is not a plugin API. The current v2 DSN/schema clone does not constitute a supported owned-connection contract: querying `current_schema()` can enter the host transaction; one schema plus `public` may not preserve effective search path/role; session advisory lock correctness needs a pinned backend; credential renewal, pooling, cancellation and cleanup are unspecified. No safe disposable-data-only host deployment was verified; **no real-host integration test was run** and no production/running worker was contacted.
- Bounded Astra Medium design outcome: **host change required for qualified principal and owned-connection semantics; bearer v2 unsupported on the current host**. Retain 401 fail-closed. Proposed host API must expose immutable account or explicitly shared installation subject, principal kind, authorization generation, account-recreation/rotation/revocation/restore rules, and a separately owned backend-pinned connection lease with documented schema/role/isolation, finite deadlines and cleanup. A future plugin/client may adopt only that reviewed contract. The separate Sol High reviewer first required three factual documentation corrections (same-second cookie risk, cookie-before-bearer precedence, idempotent release 200 versus page/catch-up 410), then returned **PASS** after correction. Configured model/effort bindings were accepted; runtime-resolved identities remain unobservable.
- Compatibility: current bearer client/new plugin retains legacy routes but v2 returns 401; future v2 client/old plugin must detect unsupported capability and preserve local state; rotated/revoked bearer cannot resume; new generation normally requires fresh bootstrap; account switch/recreation requires a different immutable subject; multiple shared-token clients would share an installation scope only under an explicit new product contract; restore/clone must invalidate unproven continuity; pre-v2 data has no invented durable checkpoint. No host, plugin, or Auralscape code was changed in Part B. Old-worker drain, populated-state cutover, LUM-008 historical client convergence, LUM-009, LUM-010 client staging/resume and release qualification remain open.

### Prior active task notes (historical)

- LUM-007 is locally implemented on the preserved dirty plugin checkout at 4276398c and independently reviewed (explicit Sol High read-only PASS). Durable safe failure categories, three-attempt transient cap with 60/300-second due slots, revision/analyzer/schema requalification, exact-source SQL eligibility, token-fenced completion/release, queued/interactive abandoned-claim recovery, and catalog-change wakeups are covered. New retry file: 28 passed (27 real PG17, one unit); affected selection: 351 passed before final atomic-arm fixes, then retry+existing 314 passed; final full serial plugin suite: 546 passed/3 unchanged published-archive identity failures. LUM-007 remains IMPLEMENTED_UNVERIFIED pending host/old-worker and release qualification. LUM-008 historical-client convergence stays open. No Auralscape edit, commit, push or deployment.
- Task 0B plugin / LUM-021 is implemented locally with scoped independent P2 PASS; finding remains IMPLEMENTING because client diagnostics and end-to-end qualification are open. Focused 10 passed and synthetic HEAD leak reproduction recorded. The published 1.2.5 archive identity gate remains open.
- Task 1A / LUM-001 code and focused PostgreSQL evidence are accepted locally after independent P1 PASS. Status is IMPLEMENTED_UNVERIFIED: no deployed candidate, existing-state consistency audit, old-worker drain, host integration or release qualification. New two-connection file: 26 passed; affected selection: 93 passed/236 deselected. Full plugin suite: 464 passed/3 failed solely against the unchanged published archive; evidence/task1a-plugin-full-v2.log.
- Task 0B client diagnostic patch in Auralscape main remains unverified and shares a dirty checkout with a separate active Option G task. Current observed client HEAD f9f8568df96e341aeecf93eaf1769c54cf6ff466; preserve all dirty/untracked work.
- Tasks 1B / LUM-002/003, 1C / LUM-004 and LUM-006 remain IMPLEMENTED_UNVERIFIED after independent scoped P1 PASS. **LUM-008 server routing is locally implemented and independently reviewed**, including tokenized attempts, durable publication, media/deletion invalidation, edge and exact-source rekey. Overall LUM-008 remains IN_PROGRESS because historical-client convergence, old-worker drain, populated-state cutover and release qualification are open. LUM-007 bounded retry lifecycle is now locally implemented and scoped reviewed; no client checkout edit, plugin commit or deployment was made.

## Decisions

1. Use two independent bounded Phase 0 assessments because producer and consumer source differ and can be inspected without overlapping writes. Terra stops after each assessment. Reviewer: Astra pending.
2. Use disposable PostgreSQL 17 matching plugin CI. Container lumae-remediation-pg-20260922, bound only to 127.0.0.1:55432, tmpfs storage, no host data mounts. Never reuse the running AudioMuse worker database. Fixed-schema integration fixture must run serially.
3. Preserve client main and all pre-existing changes as required by Auralscape AGENTS.md. Source audit distinguishes HEAD and working-tree modifications. Any implementation overlap must be isolated before editing.
4. No concurrency/prefetch performance changes until resumability and integrity gates pass. Audio-v1 semantics remain untouched pending Sol qualification.

## Evidence

- 2026-09-22: read pasted user request completely, then full governing brief. Read Auralscape AGENTS.md. No plugin/ancestor AGENTS.md found in initial scan; delegates check nested scope.
- Repository SHAs/status obtained with git rev-parse HEAD, git status --short, git branch --show-current.
- Plugin CI: Python 3.12, PostgreSQL 17, compileall, pytest tests/plugins -q, build_catalog.py --check.
- Docker client/server 29.8.0 available; postgres:17 already cached. Disposable database pg_isready: accepting connections.
- Node v24.12.0. FFmpeg 8.1.2-full_build-www.gyan.dev, gcc 16.1.0. Exact audio qualification/options not yet run.
- AudioMuse-AI plugin/api.py:142-150: enqueue delegates with job_timeout=-1. Plugin __init__.py:211-219 explicitly discards timeout. Source evidence does not qualify installed host behavior.
- Windows sandbox process startup failed apply deny-read ACLs. Automatically reviewed escalated read/test commands work; this is environment setup evidence, not a code failure.

## Deferred / unresolved

- Baseline suites and adjudication reports pending.
- P1 independent Sol review required before VERIFIED.
- Existing PostgreSQL collection browse matrix does not establish concurrent publication/revision/feed correctness.
- Host auth/CSRF/request limits, worker cancel/death/decoder stalls unexecuted. Existing running worker not modified or connected to for tests.
- Android SDK adb exists but no device mutation authorized. Physical Android/iPhone qualification remains open; this Windows host cannot provide Xcode/iOS execution.
- Browser keyboard/focus/dialog/zoom/interruption and dirty-field polling gates unexecuted.
- No matching failing production media obtained. Do not attribute PyAV failure to adjacent successful Brazil track log.
- Representative 69k analysis / 132k links / 94k profiles / 100k search baselines unexecuted. Client SQLite and server PostgreSQL costs must remain separate.
- Final selected-wave candidate SHAs, migration qualification and old/new compatibility remain open.

### Concurrent checkout change observed

During Phase 0, external work advanced Auralscape from 6501c83da0691e42c304961d580ece22debd76cb to 1f5467bc5415725e3c5ef6ff1b84f5f4da0664a6 (`feat(phase-G): Step G1 — Shared chrome, Settings and listening entry`). Remediation did not create this commit. Remaining dirty set changed. `evidence/auralscape-during-audit-status.txt` is a later snapshot, not the original 38-entry starting status. Source and tests must be stamped with observed SHA; source audit in unchanged service files may transfer only after checking diffs. Do not stage or commit unrelated client work.

## Phase 0 adjudication checkpoint

Astra inspected both reports, critical current-source paths, JUnit totals and exit status. Detailed evidence: [plugin](PHASE0_PLUGIN.md), [client](PHASE0_CLIENT.md), [environment](PHASE0_ENVIRONMENT.md).

| Disposition | Findings |
|---|---|
| PRESENT | LUM-001,002,003,004,005,006,011,012,013,014,015,016,018,020 |
| PARTIALLY_FIXED | LUM-007,008,009,010,017,019,021 |
| ALREADY_FIXED | None end-to-end; client album-ID consumer already preserves identity, plugin LUM-014 remains present |

LUM-010 has partial server paging but absent durable client resume. Performance dispositions identify current expensive query shapes and missing qualification, not measured speed claims. None is VERIFIED.

- Plugin baseline: 436 passed, 0 failed/errors/skipped, 40.814 seconds, exit 0; PostgreSQL17 disposable database. Structured evidence: evidence/plugin-baseline.junit.xml; command/output: PHASE0_PLUGIN.md and evidence/plugin-baseline.log. Compileall and release selection --check both exit0 (evidence/plugin-build-checks.json).
- Client focused baseline: 8 suites /107 tests passed. Full serial Jest rerun in progress; prior incomplete run is not a pass. Current client SHA at checkpoint 1f5467bc5415725e3c5ef6ff1b84f5f4da0664a6 plus existing dirty changes. Concurrent commit did not alter audited sync service files; settings UI evidence needs its newer SHA.
- Review correction: Terra initially classified LUM-006 ALREADY_FIXED. Astra challenged SQL IS DISTINCT FROM versus Python truthiness after LIMIT; Terra confirmed the NULL-signature starvation remains PRESENT. Existing mocked eligibility test does not expose this case. A deterministic real-PG regression remains required.
- Sol LUM-001 design accepted for implementation planning: conflict-safe initialization + per-source lock, single transaction, locked compaction, old-worker drain. See LUM-001_DESIGN.md. Independent implementation review remains required. Existing readers treat cursor==floor as valid; design corrected to preserve that behavior.

### First bounded implementation task: 0B-plugin / LUM-021

- Exact scope: database_state.py aggregate diagnostics only: stable operation/error metadata, raw-exception suppression, bounded timing of execute+fetch. No sync, cursor, job, query-selection, identity, audio or schema behavior changes.
- Model: Terra. Plugin branch will be codex/lumae-remediation-20260922; starting source SHA 4276398c671f3e2713212488f4e72a32553697d7.
- Reproduction: raw exception containing token, SQL and private path must not appear in diagnostics; deterministic timing regression for success/failure.
- Acceptance: focused database-state tests and full PG-backed regression suite; preserve existing source-scoped counts and partial results; label timing as server database execute+fetch, not network/client time.
- This task contributes to LUM-021; client per-stream outcomes and later end-to-end timings remain open. It cannot mark the entire finding VERIFIED.

### Dependency order

0B plugin diagnostics -> 1A LUM-001 implementation + independent Sol review -> 1B LUM-002/003 -> 1C LUM-004. LUM-006 then lifecycle LUM-007/008; independent client diagnostics/late preflight only after a stable source snapshot. Server resumable protocol precedes client adoption; no prefetch/concurrency optimization before resume qualification. Audio qualification is a separate versioned release. Workbench source identity precedes edition/year/search UX. Structural consolidation remains last.

### Phase 0 baseline completion

Full client Jest evidence inspected by Astra: 900 passed /1 skipped suites, 8,767 passed /3 skipped tests, 0 failures; exit0 reported and JSON success=true. Three native/runtime assertions remain OPEN: downloader session routing, iOS input diagnostic preprocessing, and Apple Swift XCTest parsing (see compact summary). Exact JSON retained locally in evidence/phase0-client/full-jest-current.json; compact tracked summary evidence/client-baseline-summary.json. Supersedes the earlier running-gate entry. Both repository baselines are now complete with explicit skips.

Task0B client accepted for bounded execution alongside independent plugin diagnostics. Only additive profile/relationship sync diagnostic records: safe operation/outcome/reason, measured durations labelled by actual boundary, imported/staged/published counts; semantic changed count unknown unless measured. No schema/protocol/recovery/admission changes. Owner Terra; main source starting1f5467bc5415725e3c5ef6ff1b84f5f4da0664a6, preserve external work. Full npm suite and redaction/partial-success regressions required before root review and local commit.


### Task0B plugin integration review — CHANGES_REQUIRED (in progress)

Initial Terra handoff was incomplete: implementation compiled, but no regression/full-suite evidence and cursor-creation failures were outside the safe diagnostic boundary. Astra inspected the diff and found missing diagnostics arguments at internal query-helper call sites, which would fail at runtime despite compilation. Returned only the scoped completion/corrections to Terra: complete call sites, safe cleanup, readable formatting, red-before-green tests, full suite and handoff. No dependent implementation unlocked; LUM-021 remains IMPLEMENTING. This review is not an independent P1 gate (Task0B is P2).

## 2026-09-23 resume checkpoint

- Recovered this ledger and Phase 0 evidence; did not repeat Phase 0. Historical Astra/Terra attribution and accepted LUM-001 and LUM-002/003 designs remain.
- Prior coordinator task 01a0c884-8b45-7cd0-a7a0-82f161a5f3b9 is idle after quota failure. Old phase0_plugin delegate 01a0c885-73b1-7ad2-8722-b8e787f591a9 completed; phase0_client 01a0c885-b2ea-7a92-9510-84eb8e2545c0 and diagnostics_completion 01a0c896-f4a1-75b1-ab88-d2a453949230 failed on quota. No prior writer remains active. A separate Auralscape Option G task is active and owns its UI changes.
- Runtime delegation controls list gpt-6-sol, gpt-6-luna, gpt-6-astra and supported Low/Medium/High effort. Trusted project .codex/config.toml now requests Sol Medium for new project turns, Luna Low as helper default and maximum two spawned threads. This active parent's effective model/effort and spawned workers' resolved identities are not exposed. The diagnostic reviewer was explicitly spawned gpt-6-sol/medium and returned scoped PASS; actual resolved identity remains UNVERIFIED. Global settings were untouched.
- Task 0B plugin corrections: safe stable query/snapshot errors, bounded execute+fetch timings shown in source-scoped HTML, safe cursor creation/rollback/close, partial-result preservation. Baseline HEAD reproduces raw-exception exposure. Independent reviewer first returned CHANGES_REQUIRED for invisible metadata, then PASS after display and regression corrections. Focused 10 passed; compileall exit 0; git diff --check exit 0.
- PG-backed full plugin suite after corrections: 438 passed, 3 failed (tests/plugins/test_release_channels.py archive/source identity against published 1.2.5 package); exit 1. This is an open release gate, not a pass. No release archive was rebuilt. Baseline plugin 436 passed and client 8,767 passed / 3 skipped remain historical evidence only.
- LUM-021 remains PARTIALLY_FIXED overall. LUM-001 remains PRESENT/IN_PROGRESS, no implementation acceptance yet. Other findings retain prior adjudication. No local commits, pushes, deployment, production DB or device changes in this resume.

### Task 1A / LUM-001 implementation checkpoint (2026-09-23)

- Explicit spawn request: GPT-6 Sol Medium implementer /root/lum001_implementation; resolved identity/effort UNVERIFIED. Fresh explicit GPT-6 Sol High reviewer /root/lum001_review; resolved identity/effort UNVERIFIED. Reviewer first returned CHANGES_REQUIRED for real-writer and boundary evidence, then PASS after the same implementer corrected coverage. No Astra escalation was required because the accepted design held.
- Production diff: catalog_enrichment.py adds conflict-safe profile state initialization, per-source FOR UPDATE for mutation, guarded head update, locked standalone compaction reread, and ascending migration source order. Existing legacy/edge publishers retain one outer transaction. No audio, source identity, relationship publication or client cursor format changed.
- Evidence: new tests/plugins/test_profile_stream_serialization.py: 26 PG17 cases pass, including two actual publishers in both orders, simultaneous first state, rollback/fault boundaries, source independence, compaction in both orders, reader commit/rollback, old/new collision characterization, and populated idempotent migration. Named stale fake fixtures in test_lumae_analysis.py updated; Task 0B test edits preserved. Affected selection 93 passed/236 deselected. compile/py_compile and git diff --check exit0.
- Full plugin suite with disposable PG17 at localhost:55432: 464 passed, 3 failed, exit1. Failures only in test_release_channels.py because modified source differs from the existing published 1.2.5 archive. Preserve that archive and gate until a separately authorized release candidate. Evidence: evidence/task1a-plugin-full-v2.log and .exitcode.txt.
- No production database or old worker was touched. Before rollout, inspect real existing state for gaps/epoch/floor consistency, drain old publishing workers, run host compatibility/security integration and release package checks. These are open, not passed. No commit/push/deploy.

### Task 1B / LUM-002/003 design gate (2026-09-23)

- Existing LUM-002-003_DESIGN.md was a design artifact, not recorded as independently accepted. Targeted explicit GPT-6 Astra Medium specialist review returned CHANGES_REQUIRED; resolved model/effort unobservable. Central row-lock/single-owner/advisory-key/fingerprint approach remains sound.
- Concrete correction: _upsert_item can update a (principal,id) item in another collection while locking/revising only the requested parent. Require ownership guard, explicit conflict and whole-request rollback, including batches. Lock a plain principal-scoped parent row before aggregate _fetch_collection. End non-2xx/replay transactions promptly; define READ COMMITTED or stricter-isolation handling; compare fingerprint version plus digest.
- Stage restore events after all collection/item writes so future LUM-004 frontier can respect lock order. Historical mutation-without-receipt cannot be retroactively guaranteed. Legacy-unbound receipts replay without mutation; old mutation workers must drain before rollout.
- Corrections appended to the existing design document, preserving original analysis. Task 1B Sol Medium implementation spawned; no collection code changed by the lead at this checkpoint.

### Task 1B / LUM-002/003 implementation checkpoint (2026-09-23)

- Explicit GPT-6 Sol Medium implementation request to /root/lum002003_implementation; explicit independent GPT-6 Sol High review request to /root/lum002003_review. Runtime-resolved model/effort cannot be observed. Specialist design review was separately requested as GPT-6 Astra Medium. One writer at a time; no nested delegation.
- Production diff in collection_manager.py makes mutation receipt admission, fingerprint version/digest, state writes and response atomic in one transaction. It locks a plain principal-scoped parent row before aggregate reads, rejects a foreign-owned item even during batches, rolls back non-2xx/faults, stages restore events after state writes, and checks READ COMMITTED after the real host's settings SELECT on the same request connection. Migrations add nullable fingerprint columns and reject partial metadata pairs; historical unbound receipts replay without mutation. The personal library label remains UTF-8 middle dot.
- Disposable PostgreSQL 17 test file tests/plugins/test_collection_mutations_postgres.py: 26 passed, including populated legacy schema migrated twice, both invalid fingerprint pairs, post-receipt failure rollback, prior settings SELECT, stricter isolation rejection, concurrent key admission, lost-response replay, ownership conflict, and concurrent CAS across six routes. Existing focused collection mocks: 7 passed. Independent reviewer initially CHANGES_REQUIRED for host isolation, CHECK null semantics and label encoding; after corrections, scoped P1 PASS with no remaining invariant gap found. This is not a release qualification.
- Full plugin suite, serial against disposable PG17: 490 passed, 3 failed, exit 1. Only tests/plugins/test_release_channels.py published 1.2.5 archive/source identity checks failed; archive was not rebuilt. Evidence: evidence/task1b-plugin-full-v2.log and .exitcode.txt. git diff --check exit 0 after normalizing only added lines in the shared test file.
- Open gates: old mutation workers drain; real host integration and existing-state migration/compatibility; LUM-004 commit-order frontier with late-commit regression; candidate archive/release checks. No plugin commit, push, deployment, production database or device mutation. Client checkout remains owned by a separate active task.

### Task 1C / LUM-004 implementation checkpoint (2026-09-23)

- Bounded read-only publication/cursor architecture request to GPT-6 Astra Medium /root/lum004_design chose a transactionally allocated singleton global feed counter/frontier. Explicit GPT-6 Sol Medium implementation /root/lum004_implementation; separate explicit GPT-6 Astra Medium independent P1 reviewer /root/lum004_review returned PASS after focused correction. Resolved model/effort is not observable. No nested delegation or concurrent writers.
- collection_manager.py adds persistent protocol version/epoch/head_seq, seeds from MAX(committed historical seq) under a change-table lock, retains the prior transaction-local timeouts after the seed, and preserves epoch/head on repeat migration. Every event takes its numeric seq from UPDATE head_seq=head_seq+1 RETURNING inside the mutation transaction and inserts it explicitly, never using BIGSERIAL default. State, events, receipt and head commit/roll back together. Reader captures head and returns only authenticated-principal events with cursor<seq<=head; next_cursor remains last returned seq or input cursor. Missing/unknown frontier fails closed. Existing restore stages all state writes before publication.
- New tests/plugins/test_collection_feed_postgres.py exercises late commit and observed database blocking across collections/principals, rollback number reuse, missing/unknown frontier, populated CACHE>1 migration twice, two-collection restore atomicity, receipt replay, pagination/tombstone and commit between reader's head and event reads. Existing migration mocks updated narrowly. Focused serial affected selection: 54 passed/256 deselected; independent specialist P1 PASS. git diff --check exit 0.
- First full PG17 run: 492 passed/5 failed; two stale migration mocks plus three published archive identity failures. Corrected mocks and timeout leakage, re-reviewed PASS. Final full serial plugin suite: 495 passed/3 failed, exit 1; remaining failures only tests/plugins/test_release_channels.py comparison with unchanged published 1.2.5 archive. Evidence: evidence/task1c-plugin-full-v2.log and .exitcode.txt. Archive not rebuilt.
- Release gates: all old collection writers/transactions/pools must drain before migration/cutover; real existing-state and host installation compatibility; measured global-frontier contention; explicit client reconciliation for any events already missed behind legacy numeric cursors while preserving pending edits/manual shelves/order/undo; candidate release archive. Rolling back to old writer code needs a write pause, sequence realignment and epoch/cursor reconciliation. No commit, push, deployment, production data or device changes.

### LUM-006 backfill eligibility checkpoint (2026-09-23)

- Explicit GPT-6 Sol Medium implementation /root/lum006_implementation; separate explicit GPT-6 Sol High P1 reviewer /root/lum006_review. Runtime-resolved model/effort unobservable. Reviewer first CHANGES_REQUIRED for missing current catalog fingerprint sentinel; corrected SQL/Python guards then independent PASS. No concurrent writer or nested delegation.
- Prior confirmed cause: SQL included a ready row with NULL stored media_signature via IS DISTINCT FROM before LIMIT, but Python rejected it afterward, starving later eligible rows. Minimal fix in __init__.py: Python ready mismatch accepts missing stored signature only when current signature is known; SQL ready mismatch requires nonempty t.media_fp; Python rejects synthetic catalog-media: sentinel when media_fp is NULL/empty. Pending, failed, skipped, version and source scoping remain unchanged.
- New fake-row and disposable PG17 regressions in test_lumae_analysis.py cover LIMIT-1 progress with NULL/empty stored signatures and with NULL/empty current media_fp, global and source-scoped selection, then renewed eligibility when a real fingerprint arrives. Initial targeted red 4 failures; reviewer-edge red 4 failures; final focused backfill selection 23 passed/264 deselected. Decision in LUM-006_DESIGN.md. git diff --check exit 0.
- Full serial plugin suite against disposable PostgreSQL 17: 503 passed, 3 failed, exit 1. Only tests/plugins/test_release_channels.py archive/source identity checks against unchanged published 1.2.5 failed. Evidence: evidence/lum006-plugin-full-v2.log and .exitcode.txt. No archive rebuilt. Finding is IMPLEMENTED_UNVERIFIED until host/existing-state and release qualification; LUM-007 failed/repaired media lifecycle remains separate. No commit, push, deployment, production data or device changes.

### LUM-008 published-profile foundation checkpoint (2026-09-23)

- Bounded GPT-6 Astra Medium specialist lifecycle design /root/lum007008_design recommended durable published baseline separate from mutable attempt/job state, before broadening LUM-007 retries. Explicit GPT-6 Sol High migration implementation /root/lum008_projection_seed and separate GPT-6 Astra Medium independent foundation review /root/lum008_seed_review returned PASS. Runtime-resolved models/efforts unobservable; one writer, no nested delegation.
- Additive migration in __init__.py creates published_source_profiles keyed by exact catalog_instance_id/track_id with cascading source FK and typed baseline fields. Distinct profile_migrations marker published_source_profiles_seed_v1 atomically copies only current ready source rows after the legacy-default ownership migration. Source identity, original bytes/signature/schema/analyzed_at retained; failed/pending/stale/skipped rows are not guessed. Repeating migrate never reseeds a removed published row. Runtime read/write routes are deliberately unchanged at this checkpoint; do not deploy this partial foundation.
- Two real disposable PG17 migration cases and two existing migration mocks passed (focused 4 passed/285 deselected). Independent specialist review PASS for foundation only; py_compile and git diff --check passed. Full serial plugin suite 505 passed/3 failed, exit 1; only published 1.2.5 archive/source identity failures remain. Evidence: evidence/lum008-seed-plugin-full.log and .exitcode.txt; unchanged archive preserved.
- Open LUM-008 work: tokenized attempt admission and completion fence; atomic published-row/stream writes; same-revision pending/failure preservation; edge publisher and exact-source rekey integration; explicit delete on authoritative media-revision invalidation; historical-client convergence. Auralscape bootstrap merges rows and cannot clear absent cached baselines via epoch reset alone. Old worker drain and atomic cutover are required because the one-shot seed marker cannot bridge writes between seed and routing. No commit, push, deployment, production data or device changes.


### LUM-008 attempt/publication routing checkpoint (2026-09-23)

- Resumed the existing one-shot ready seed; no Phase 0 repeat or reset. The lead remained the sole writer. Explicit independent GPT-6 Sol High reviewer context returned initial P1/P2 findings, then PASS after corrections. Configured reviewer model/effort was accepted by the delegation tool; actual resolved worker identity and parent model remain unobservable. No Terra work and no nested delegation.
- Added nullable attempt token, captured media revision, catalog epoch, analyzer/schema contract and start timestamp to source_profiles. Each source claim uses the catalog-state lock and a UUID token. Queued batches, direct backfill, hooks, release, stale recovery and completion carry or fence that token. Tokenless persisted jobs cannot publish or release a newer claim. Unknown catalogue media fingerprints enter durable deferred_no_media_revision and re-enter backfill when a real fingerprint appears.
- The published_source_profiles table is the public baseline for /api/profiles, bootstrap and edge. Same-revision pending/failure retains its bytes, signature and timestamp; identical ready refresh emits no event. Changed ready payload, its edge invalidation, the published row, profile event and stream head commit together. Catalog publication withdraws known changed/deleted media with a delete event; fingerprint-schema rebase reconciles all published rows. Missing current or stored signature alone does not prove replacement.
- Exact-source provider rekey moves published identity, fences attempts, removes old-key edge payload/jobs and journals delete(old)/upsert(new) in the same transaction. The independent review identified and confirmed correction of rekey stream, rebase, edge/delta and unknown-revision retry gaps.
- New real PG17 tests in tests/plugins/test_profile_publication_postgres.py: 13 passed. Existing migration seed cases: 2 passed; two-connection serialization/rollback cases: 26 passed. Final serial full plugin suite: **518 passed, 3 failed**, exit 1; see [runtime evidence](evidence/lum008-runtime-checkpoint.md); only unchanged published 1.2.5 archive/source identity tests in test_release_channels.py. Archive was not rebuilt. scripts/build_catalog.py --check remains an open gate: published versions are immutable and a new version is required. git diff --check passed. Disposable PG17 only; no host/prod database or running workers touched.
- Overall LUM-008 stays IN_PROGRESS. Existing Auralscape cached historical profiles cannot be converged by an epoch reset or absence-only bootstrap; the client checkout was not edited in this task. Old worker drain, populated-state migration/cutover, host compatibility, full release archive and client historical convergence remain open gates. The one-shot seed marker must not be reused to bridge old-writer activity.


### LUM-007 bounded repaired-media retry checkpoint (2026-09-23)

- Resumed at exact preserved plugin HEAD 4276398c on codex/lumae-remediation-20260922. No Phase 0 repeat, reset, commit, push, production DB, running worker, release archive, audio/protocol version or Auralscape edit. Project model config requests Sol Medium lead and Luna Low default helper; explicit gpt-6-sol High reviewer accepted and returned read-only PASS. Runtime-resolved identities remain unobservable; no Terra delegation.
- Existing LUM-008 source_profiles attempt token remains the sole completion/release authority. Additive migration adds safe retry category, count, due timestamp, captured media signature and analyzer/schema versions, plus a durable backfill wake flag. Pending/interactive claim and recovery wake arm in one transaction; failure rolls both back. Accepted ready completion clears retry metadata and publishes baseline/stream atomically. Failure preserves the same-revision published baseline.
- Transient download/missing media/timeout/unknown failures have three total attempts with 60/300-second cooldown and no automatic fourth attempt. Silent/invalid/unsupported/resource-bound media waits for a known media revision or contract upgrade. Queue-release and abandoned-token recovery share the finite cap. Unknown fingerprints defer without inferring a replacement. A known repaired media fingerprint or analyzer/schema upgrade requalifies the exact source; legacy global compatibility remains separate.
- The bounded backfill selector filters cooldown/exhaustion before LIMIT. The workflow retains the earliest due retry or pending-token expiry; it cannot mark itself complete during the 30-minute worker-reclaim / one-hour claim-expiry gap. Catalog publication rearms changed sources, including changes racing a running batch; no-change refresh performs a bounded recheck to recover a failed wake. Removed/ineligible tracks do not perpetually rearm.
- New tests/plugins/test_profile_retry_postgres.py: 28 passed (27 on disposable PG17 and one bounded unit), covering taxonomy/redaction, cooldown/cap, repaired revision, contract upgrade, missing media, published-baseline retention, token fences, queue release, stale interactive/background recovery, due and catalog wakeups, no-change fallback, atomic arm rollback and ready publication. Existing tests/plugins/test_lumae_analysis.py: 287 passed after fixture/schema updates. Affected retry/publication/stream/migration selection: 351 passed before final atomic-arm changes; final complete serial suite: **546 passed, 3 failed**, exit 1, only unchanged published 1.2.5 archive/source identity cases. git diff --check and py_compile passed. No archive rebuilt.
- Independent Sol High reviewer initially found stranded delayed retries, catalog revision wake, pending-interactive/30-minute recovery and admission/arm crash window. The lead corrected each and added PG17 regressions; final scoped review PASS. This is local implementation evidence, not release verification. Retain old-worker drain, host/populated-state qualification and immutable published-version release gate. LUM-008 historical Auralscape cached-row convergence remains independently open.
