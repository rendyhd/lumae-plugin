# LUM-010 disposable integration qualification — 2026-09-24

## Frozen implementation and isolation

- AudioMuse-AI: `89f2c9d43f0d9eaedfdf87bfc09e50299935d2bd`.
- Lumae implementation: `4b547927c60ec5aad9f728fb6b67d46c836233a8`; bookkeeping/source checkout: `dbf9297e55490f05406e5ac6b056d10a2a091c92`.
- Auralscape: `de4ad73f8c674335f4cb23628af1b4cfd8409cf9`.
- Qualification branch: `codex/lum010-qualification-20260924`, based on the plugin bookkeeping commit. Frozen implementation worktrees remained clean. A temporary test copy in the frozen Auralscape checkout was removed after execution; the test source is retained here.
- Compose project `lum010qual20260924` used cached `postgres:17` and `redis:7-alpine` images with a project-only network, loopback host ports 55259/55260, PostgreSQL data on 4 GB tmpfs, Redis persistence disabled, and no bind mount or production database. Real AudioMuse host ran only on `127.0.0.1:55261`; clone host ran only on `127.0.0.1:52191`. Its configured Navidrome URL was an unused loopback port 55262 with synthetic credentials. No media provider or real worker was run.
- Credentials, tokens, raw logs, Jest JSON, and the SQLite fixture live only in the private sibling directory `lum010-qualification-runtime-20260924`, outside Git. The harness now requires exact clean frozen source paths/SHAs, that exact disposable directory, and the PostgreSQL 17 cluster system identifier/database/role/data directory before any migration or fixture write. The Compose project and PIDs were checked before stopping or recreating them.

The first 512 MB PostgreSQL tmpfs filled during a broad test run; PostgreSQL logged `No space left on device` and shut down. That run did not qualify the suite. The same isolated project was recreated with 4 GB tmpfs, a new cluster identifier was captured privately, and the integration checks below were repeated. No existing local containers were stopped.

## Executed evidence

| Gate | Result | Boundary exercised |
|---|---|---|
| Host/plugin migrations | PASS | Real host `database.init_db()` and plugin `migrate(db)` ran twice on the disposable PG17 database. Real host plugin registry subsequently reported `lumae_analysis` enabled and `load_status=ok`. Fresh and restored populated clone startup both completed. |
| Real HTTP account v2 | PASS | `/auth` issued a durable account binding; health advertised v2; create captured two synthetic published rows with page size one. Host PID changed from 22740 to 59540. The saved cookie and token replayed the identical first page after restart, then read the second page. Catchup pinned a two-event head (upsert payload and delete tombstone); a later third event was excluded. Exact first catchup replay, release and post-release 410, cross-account 410, bearer-only 401, malformed token/protocol 400, and zero remaining advisory locks passed. `http_qualification.py` is the executable evidence. |
| Frozen Auralscape real-host integration | **2 passed / 0 failed** | Actual `AudioMuseClient` account login, capability selection, v2 create/page/catchup/release over loopback HTTP. Actual `runProfileV2Bootstrap` and SQLite staging persisted one page; an injected second-page transport interruption left a historical published row untouched; a new client with a reopened SQLite file resumed the same session and atomically published tracks `a,c,d` while withdrawing historical `b`. The test uses a Node cookie shim, passes a no-op admission guard, and supplies a test mock for the publication-source predicate. It does not exercise live LUM-009 admission or native cookie behavior. Test: `client_http_qualification.test.ts`. |
| Disposable clone/restore | PASS for same-cluster characterization | `pg_dump -Fc` of the synthetic database, `createdb lum010_clone`, and `pg_restore` inside the disposable container. A second real host booted against that clone. The copied account principal, catalog/profile epochs, snapshot sequence/count and first page replayed exactly with the original cookie/token and unchanged synthetic JWT secret. Releasing the clone session did not release the original session. Both plugin registries reported `load_status=ok`; each database retained three synthetic published rows and zero advisory locks. `clone_qualification.py` and `host_clone_http.py` are executable evidence. This is not a production restore or cross-host secret-rotation guarantee. |
| Frozen full plugin suite, rebuilt PG17 | **567 passed / 3 failed**, exit 1 | All three failures are `test_release_channels.py` checks comparing modified source with the unchanged published 1.2.5 archive. The archive was neither rebuilt nor replaced. Raw result: private `plugin-full-v5.log` and `.exitcode.txt`. |
| Frozen full Auralscape Jest | **912 passed / 1 failed / 1 skipped suites; 8,905 passed / 1 failed / 3 skipped tests**, exit 1 | The single failure is `scripts/__tests__/performance-continuity.test.js`: its SQL extraction expands `${SHELF_SCHEMA_SQL}` but leaves the newer `${PROFILE_PUBLICATION_SCHEMA_SQL}` and `${PROFILE_BOOTSTRAP_V2_SCHEMA_SQL}` placeholders, yielding SQLite `unrecognized token: "$"`. This existing test script was not changed in the frozen client checkout. Raw result: private `client-full-v2.log`, `.json`, and `.exitcode.txt`. |
| Host auth unit evidence | Recorded prior checkpoint | The exact frozen host SHA already had 94 auth unit passes. This qualification exercised its real HTTP middleware, but did not rerun the whole host unit suite. |
| Harness preflight and independent review | PASS | All seven Python harness modules compiled; `python -O preflight_negative.py` rejected a forged work directory and PostgreSQL cluster identifier before mutation. A separate configured `gpt-6-sol` High reviewer checked the bounded evidence and returned PASS after preflight, restart, payload and client SQLite assertions were strengthened. Runtime-resolved model identity was not observable. |

## Open gates and limits

- **Rollout:** no real old publishing worker was drained. Unit and PG fixture tests characterize stale completions, but active old-worker cutover, lock ordering across versions, live populated-state migration, rollback and historical collection-cursor reconciliation remain OPEN.
- **Client admission:** the real SQLite bootstrap test passes a no-op admission guard and provides a publication-source predicate mock. The existing frozen client unit suite covers admission behavior separately, but native provider proof, shared native cookie jar and device behavior against this host remain OPEN. The synthetic source was created by host startup and its published profile/event fixture was inserted directly into the disposable database; this run did not exercise real media analysis, provider scan, edge audio qualification or journal compaction under production load.
- **Restore:** same-cluster dump/restore with the same synthetic JWT secret passed. Cross-cluster, secret rotation, old-host rollback, actual installation migration and populated personal outboxes/shelves/order/undo are OPEN.
- **Release:** the three immutable published 1.2.5 archive checks and the one frozen-client performance-continuity test remain failing gates. Full host-suite, native/device, audio, mixed-version release and deployment gates remain OPEN. Bearer-only v2 is intentionally unsupported by this protocol.

Both verified disposable host processes were stopped, and only Compose project `lum010qual20260924` was taken down after evidence collection. Its PostgreSQL tmpfs state was discarded; private synthetic credentials and raw logs remain outside Git for local inspection. No production data or service was touched. No push, tag, deployment, release version selection, or published archive rebuild occurred. This evidence qualifies only the scenarios explicitly listed above; LUM-010 remains implemented but not release qualified.
