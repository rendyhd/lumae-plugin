# Lumae remediation — resume checkpoint (2026-09-23)

The authoritative overview is [LUMAE_REMEDIATION_LEDGER.md](LUMAE_REMEDIATION_LEDGER.md). Preserve task IDs 0A/0B, 1A–1C and LUM-001–021, existing decisions, historical evidence and review attribution. Phase 0 is complete. The user’s V2 brief replaced model routing and startup only.

## Exact implementation checkpoint and checkout

- Plugin path: `C:\Users\rendy\vscode\lumae-plugin`; branch `codex/lumae-remediation-20260922`.
- **SERVER_REMEDIATION_CHECKPOINT_SHA:** `816420f944bdac02c620928e46d752fde981d577` (local commit `remediation: checkpoint server integrity and profile lifecycle`). It contains LUM-001/002/003/004/006/007, server-side LUM-008, plugin-side Task 0B/LUM-021, associated migrations, tests and design/implementation evidence. This is the immutable implementation baseline, **not a release-qualified SHA**. Commit 1 was local-only and was not pushed.
- **LUM010_START_SHA:** `95f56298e4d3c91230c66c1fcffdb75f05c2afb5` (separate local bookkeeping commit `docs: record remediation checkpoint`). This is the LUM-010 orchestration/resume baseline. At LUM-010 entry, HEAD matched it and `git status --short` was empty. This value is recorded at the first normal LUM-010 documentation update; neither checkpoint commit was amended or pushed.
- **LUM010_SERVER_CHECKPOINT_SHA:** `a713e33f6550f769256f85e06f186298c8875f28` (local-only `remediation: checkpoint resumable profile bootstrap server`). It contains the reviewed LUM-010 server implementation, migrations, tests, design, and compact evidence. It is not host-, client-, or release-qualified and was not pushed. The previous two checkpoint commits were not amended.
- **LUM010_HOST_START_SHA:** `a62f72e2d8595601171ec4e2bf2a197ca735ed44`, the separate local-only `docs: record LUM-010 server checkpoint` bookkeeping commit. HEAD matched and `git status --short` was empty at Part B entry. This exact SHA is recorded at the first normal host-task documentation update; earlier commits were not amended or pushed.
- The pre-commit implementation checkout had no unstaged tracked code differences from the staged checkpoint. Affected Python compileall/py_compile and both unstaged/staged `git diff --check` passed. No plugin source changed after the recorded full-suite run. Full serial disposable PostgreSQL 17 plugin suite: **546 passed, 3 failed**, exit 1; the three are unchanged published 1.2.5 archive/source identity checks and remain open release gates. No archive rebuild or new version selection.
- Project `.codex/config.toml` intentionally requests GPT-6 Sol Medium lead, Luna Low default helper and at most two spawned threads. Actual resolved parent/delegate model and effort are unobservable. Global settings were not changed; no Terra work was scheduled. Previous independent P1 reviews and accepted designs remain in the ledger.

## Remaining gates and next safe action

- **Active result: LUM-010 host identity / owned-connection qualification HOLD.** Bounded Astra Medium host design and fresh Sol High independent review (CHANGES_REQUIRED for three documentation facts, corrected, final PASS) are recorded in [LUM-010_HOST_CONTRACT.md](LUM-010_HOST_CONTRACT.md). Configured models/efforts were accepted; runtime-resolved identities are unobservable. The server implementation remains locally checkpointed but unverified for host/client/release. Auralscape was not edited.
- Real-host integration, old-worker/writer drain, populated-state migration and compatibility, legacy feed-cursor reconciliation, historical Auralscape cached-profile convergence for LUM-008, broader client/integration and release qualification remain open. LUM-021 client diagnostics remain open. Existing personal state, principal/source boundaries, epochs, tombstones, atomic publication and versioned audio/protocol semantics must be preserved.
- Auralscape was previously observed dirty and separately owned; no Auralscape files were edited in this checkpoint. Other plugin worktrees remain untouched. Local ignored full-Jest evidence under `docs/remediation/evidence/phase0-client/` is retained without tracking the large logs/JSON.
- Final focused disposable PG17 selection: **54 passed**, including 13 new LUM-010 tests. Full serial plugin suite before the last test-only bearer regression: **558 passed, 3 failed** in 68.77 seconds; the three are only the unchanged published 1.2.5 archive/source identity release gates. Affected Python compilation and `git diff --check` passed. No archive rebuild or version selection.
- Part B began at clean `LUM010_HOST_START_SHA` `a62f72e2d8595601171ec4e2bf2a197ca735ed44`. Only this ledger, RESUME, and the new host-contract note are intentional documentation changes; no source code changed. Other plugin worktrees and Auralscape remain untouched.
- Read-only AudioMuse-AI host source at `f100684b1753e303f2b698b4f56f5b1b8972a5bb` sets `g.auth_user=None` for bearer admin requests. V2 correctly returns 401 without database access, as a new focused route test confirms. Astra Medium accepted this fail-closed behavior; bearer-based client adoption needs a separately reviewed stable principal identity and rotation/revocation contract. Host connection behavior remains unqualified.
- Host source `f100684b1753e303f2b698b4f56f5b1b8972a5bb` exposes a request-scoped psycopg2 `get_db`, but no public immutable principal resolver or owned connection lease. Shared bearer has admin authority and no account user; cookie identity is a username and same-second password change/delete-recreate can preserve an old cookie. The current DSN/schema clone is not a qualified public connection contract. Bearer v2 remains 401 fail-closed; account v2 lifecycle is also unqualified. No safe disposable-data-only host deployment was verified, so real-host tests were not run. The accepted outcome is a separately reviewed host API contract for immutable account or explicit installation subject, authorization generation and owned backend-pinned connection with schema/role/isolation, finite timeouts/cancellation and cleanup. Do not substitute `__global__`, IP, token hash or username for that contract.
- Next safe action requires a separately authorized host API change or a verified isolated host test deployment that can satisfy the contract. Retain old-worker drain, populated-state cutover, LUM-008 historical client convergence, LUM-009, LUM-010 client durable staging/resume and release qualification as open gates. Do not push, tag, deploy, modify production data, rebuild the published 1.2.5 archive or choose a release version. No work continues after this session ends.
