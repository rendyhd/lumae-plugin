# Lumae remediation — resume checkpoint (2026-09-24)

The authoritative overview is [LUMAE_REMEDIATION_LEDGER.md](LUMAE_REMEDIATION_LEDGER.md). Preserve Phase 0, accepted decisions, task IDs, historical evidence, and prior review attribution. The user’s V2 brief replaced model routing and startup instructions only.

## Current checkpoint: LUM-010 disposable qualification

- **Worktree:** `C:\Users\rendy\.codex\visualizations\2026\09\23\01a0cd25-b1b9-7892-a5e2-a3079b969a69\lumae-lum010-qualification`, branch `codex/lum010-qualification-20260924`, based on `dbf9297e55490f05406e5ac6b056d10a2a091c92`. This branch contains qualification harness/evidence and ledger/RESUME changes only. The commit containing this update is the local qualification evidence checkpoint; resolve its exact SHA with `git rev-parse HEAD` rather than amending it into itself. Frozen host `89f2c9d43f0d9eaedfdf87bfc09e50299935d2bd`, plugin implementation `4b547927c60ec5aad9f728fb6b67d46c836233a8` (bookkeeping `dbf9297...`), and Auralscape `de4ad73f8c674335f4cb23628af1b4cfd8409cf9` remain clean. Shared Lumae `.codex/config.toml` was pre-existing and untouched.
- **Accepted qualification evidence:** [full matrix and limits](evidence/lum010_qualification/QUALIFICATION.md). Real account HTTP v2 create/page/catchup/release replayed after a verified host restart; frozen Auralscape `AudioMuseClient` and `runProfileV2Bootstrap` with reopened SQLite passed **2/2** integration tests and atomically withdrew a historical profile; a same-cluster PG dump/restore clone replayed the account-bound session on a second real host. The client test passes a no-op LUM-009 admission guard and uses a Node cookie shim, so native/provider admission remains open.
- **Broader tests:** rebuilt disposable PG17 full plugin suite **567 passed / 3 published-archive identity failures**, exit 1. Frozen full Auralscape Jest **8,905 passed / 1 failure / 3 skipped** (912 passed / 1 failed / 1 skipped suites), exit 1; the failing performance-continuity script leaves new SQL template placeholders unexpanded. The first disposable 512 MB PG tmpfs exhausted space; it was recreated at 4 GB before accepted results. The prior host auth 94 passes are historical unchanged-source evidence, not a new tally.
- **Still open:** active old-worker drain, pre-remediation populated migration/cutover/rollback, legacy collection cursor reconciliation, real LUM-009 admission, native/device cookie behavior, cross-cluster restore and secret changes, full host suite, audio/mixed-version and release archive gates. LUM-010 remains **IMPLEMENTED_UNVERIFIED** for release. No product change, production access, push, tag, deployment, 1.2.5 rebuild or version selection. Do not proceed to performance work in this bounded task.
- **Review and teardown:** a separate configured `gpt-6-sol` High reviewer returned **PASS** after evidence corrections; resolved model identity was not observable. Harness Python compilation and optimized-Python negative preflight passed. Both verified disposable host processes were stopped and only the qualification Compose project was taken down; its PostgreSQL tmpfs state was discarded. Raw logs and synthetic credentials remain in the private sibling runtime directory, outside Git.
- **Next safe action:** stop this bounded qualification task after checking the local evidence checkpoint SHA and clean worktree. A later separately authorized task can address the documented rollout, native/client, populated-state, restore and release gates. No work continues after this session.

## Previous checkpoint: LUM-010 client local implementation

- **Exact worktrees.** Auralscape `C:\Users\rendy\.codex\visualizations\2026\09\23\01a0cd25-b1b9-7892-a5e2-a3079b969a69\Auralscape-lum008-client`, branch `codex/lum010-client-resume-20260924`, is clean at `LUM010_CLIENT_CHECKPOINT_SHA` `de4ad73f8c674335f4cb23628af1b4cfd8409cf9`. It began exactly at LUM-009 `9fa3f572cc3d97df4dbd8f8369e593e43ea4a432`; earlier client checkpoints were not amended. AudioMuse-AI isolated binding worktree `AudioMuse-lum010-binding`, branch `codex/lum010-auth-binding-20260924`, is clean at `89f2c9d43f0d9eaedfdf87bfc09e50299935d2bd`. Plugin isolated worktree `lumae-lum010-capability`, branch `codex/lum010-profile-capability-20260924`, has implementation baseline `4b547927c60ec5aad9f728fb6b67d46c836233a8`. The commit containing this bookkeeping update is the current local resume HEAD; resolve its exact SHA after committing rather than amending it into itself. The shared Lumae checkout still has its pre-existing `.codex/config.toml` edit, untouched.
- **Active result.** LUM-010 account-only client producer/consumer is locally implemented and independently reviewed PASS, but **IMPLEMENTED_UNVERIFIED** for release. The host `/auth` binding and plugin create echo close the shared-cookie account race. The client persists exact source/principal/session/page/finite-head state, atomically stages pages and progress through LUM-008, fences LUM-009 admission, and publishes only after server-validated finite catch-up. A successful server replay governs over local clock skew. Bearer v2 remains unsupported; legacy endpoints remain available. The plugin health capability and old-host login fallback are part of the client adoption contract.
- **Evidence.** Frozen-source full serial Auralscape Jest: 909 suites/8,879 tests passed; 4 suites/27 tests failed only on known sandbox `spawnSync EPERM`; 1 suite/3 tests skipped. TypeScript, changed-file functional ESLint and staged diff check passed. AudioMuse auth unit suite: 94 passed. Plugin health selection: 20 passed; PostgreSQL create-echo case skipped without a disposable DB. Independent Sol High review first required account/clock fixes, then a mixed-rollout delta-410 fallback correction, and returned PASS on the recheck. Configured model was `gpt-6-sol` High; runtime resolution was not observable. The historical server 566-pass/3 published-archive identity failure evidence is unchanged.
- **Open gates.** Client-to-real-host disposable integration was unavailable (`LUMAE_POSTGRES_TEST_DSN` and `LUMAE_HOST_SOURCE` unset). Host/plugin integration and rollout, old-worker drain, populated-state cutover/rollback, restore/clone continuity, bearer/installation-principal v2, native/device qualification, four sandbox-blocked Jest suites and release candidate/archive qualification remain open. No push, tag, deployment, production data change, 1.2.5 archive rebuild, or release-version selection occurred.
- **Next safe action.** Stop this bounded LUM-010 client task. On a separately authorized integration task, establish a verified disposable host/plugin/PostgreSQL harness, then test a real client restart/catch-up/release and remaining cutover boundaries. Do not begin performance work automatically.

## Historical host-adapter checkpoint (preserved)

## Exact checkout and checkpoints

- Plugin: `C:\Users\rendy\vscode\lumae-plugin`, branch `codex/lumae-remediation-20260922`. The task entered at clean HEAD `adf9a56a83f471fa5bf747d0b6b2aed47ba3791c`. The reviewed AudioMuse-AI worktree `C:\Users\rendy\vscode\AudioMuse-AI\.codex-worktrees\lum010-host-plugin-api` was clean at `3d6b40c8d8417e6907ca8dfc645ea19fd2a552ca`; neither it nor Auralscape was edited in this task.
- `SERVER_REMEDIATION_CHECKPOINT_SHA` = `816420f944bdac02c620928e46d752fde981d577`, local-only server integrity/profile-lifecycle implementation baseline. `LUM010_START_SHA` = `95f56298e4d3c91230c66c1fcffdb75f05c2afb5`, its separate bookkeeping/orchestration baseline.
- `LUM010_SERVER_CHECKPOINT_SHA` = `a713e33f6550f769256f85e06f186298c8875f28`, local-only resumable-bootstrap server implementation. `LUM010_HOST_START_SHA` = `a62f72e2d8595601171ec4e2bf2a197ca735ed44`, separate bookkeeping baseline.
- **`LUM010_HOST_ADAPTER_CHECKPOINT_SHA` = `9914d8562c5f12d77f44a67165824588041e0fc1`.** This local-only implementation commit adopts public host account principal and owned DB lease, invalidates provisional username sessions, and includes tests, design, and disposable host integration evidence. It was not pushed and is not a host deployment or release-qualified SHA. The separate commit containing this bookkeeping update is the next orchestration/resume baseline; resolve its exact SHA after committing and do not amend it to insert its own SHA.
- Immediately after the implementation commit, `git status --short` showed only the intentional ledger edit. This RESUME edit and that ledger edit are the only intended bookkeeping changes before the second commit. Recheck HEAD and clean status after that commit.

## Active task and accepted result

**LUM-010 account-only host adapter: locally implemented, independently reviewed PASS, not release-qualified.** The plugin uses `plugin.api.get_principal()` for immutable account subject/kind/authorization generation and `plugin.api.open_db_connection()` for a fresh pinned owned backend. It never commits or rolls back the request connection. Bearer-only/auth-disabled v2 remains 401; old hosts lacking either API keep legacy routes importable and return bounded 503 for v2. Migration invalidates unreleased username-bound v2 sessions and preserves legacy profile data. Ordinary process restart retains account sessions; restore/clone continuity remains open. Preserve source/principal boundaries, epochs, tombstones, atomic publication, fixed page size, and versioned protocol semantics.

The separately configured `gpt-6-sol` Medium writer completed the adapter; a fresh `gpt-6-sol` High reviewer required one P1 correction to the disposable evidence harness, then returned **PASS** after recheck. Runtime-resolved model identity and effort are not observable. No nested delegation or concurrent source writers were used.

## Evidence and open gates

- Dedicated disposable PostgreSQL 17, serial plugin suite: **566 passed, 3 failed**, exit 1; failures are only the unchanged published 1.2.5 archive/source identity release checks. Focused LUM-010 PostgreSQL file: **20 passed**. Earlier affected selection: **133 passed, 241 deselected**, before two lease-error cases. Targeted `py_compile`, unstaged/staged `git diff --check`, and staged-tree comparison passed. Do not rebuild the 1.2.5 archive or select a new version.
- [Real-host integration harness](evidence/lum010_host_adapter_integration.py) ran against the reviewed isolated host with actual account middleware and public API. It passed account create/page/replay/finite tombstone catch-up/release, cross-account isolation, role change, password-generation rotation, username recreation, bearer rejection without a write, separate backend/schema/role, unchanged request transaction, and fresh-app replay. Its preflight verifies exact host SHA/clean state, disposable DSN and connected server before mutation; optimized-Python negative checks rejected a misleading DSN and preserved an existing role search path. It cleaned its disposable schema and role setting. This does not qualify a deployed installation or database restore/clone.
- The host’s earlier **98 focused** and **268 affected** passes are separate evidence; its full unit suite is incomplete. Old-worker/writer drain, populated-state migration and rollback compatibility, host API merge/installed-host qualification, restore/clone continuity, historical Auralscape LUM-008 client convergence, bearer/client durable staging/resume, LUM-009, broader integration and release checks remain open. Personal outboxes, manual shelves/order/undo, legacy feed cursors and audio/protocol semantics remain protected.
- No push, tag, deployment, production data change, published archive rebuild, release version selection, or Auralscape edit. No work continues after this session ends.

**Next safe action:** resume LUM-010 from the clean bookkeeping commit after confirming its SHA and status; qualify the host API integration and populated-state/old-worker boundary before client adoption or release claims. Do not repeat Phase 0 or either earlier checkpoint.
