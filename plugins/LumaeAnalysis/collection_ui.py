"""Art-first web workbench for Living Collections."""

from html import escape


def render_collection_workbench(principal_label, principal_detail):
    html = r"""
<style>
  .collections-page{--ink:#efeae0;--muted:#aaa296;--quiet:#777064;--gold:#d4a54a;--gold-2:#edc774;--bg:#15130f;--panel:#211e18;--raised:#29251e;--control:#332d24;--line:#4b4235;--danger:#ffb4a8;color:var(--ink);color-scheme:dark;font-family:Inter,ui-sans-serif,system-ui,sans-serif;padding-bottom:88px}
  .collections-page *{box-sizing:border-box}.collections-page [hidden]{display:none!important}.collections-page button,.collections-page input,.collections-page select,.collections-page textarea{font:inherit}.collections-page button{min-height:42px;border-radius:10px;border:1px solid var(--line);background:var(--control);color:var(--ink);padding:9px 14px;font-weight:700;cursor:pointer}.collections-page button:hover{border-color:#776953;background:#40372b}.collections-page button:focus-visible,.collections-page input:focus-visible,.collections-page select:focus-visible,.collections-page textarea:focus-visible{outline:3px solid #d4a54a66;outline-offset:2px}.collections-page button.primary{background:var(--gold);border-color:var(--gold);color:#19150e}.collections-page button.primary:hover{background:var(--gold-2)}.collections-page button.ghost{background:transparent}.collections-page button.danger{color:var(--danger)}.collections-page button:disabled{opacity:.42;cursor:not-allowed}.collections-page input,.collections-page select,.collections-page textarea{width:100%;background:#17140f;color:var(--ink);border:1px solid #5d5243;border-radius:10px;padding:11px}.collections-page img{display:block}
  .collections-page a.button-link{min-height:42px;display:inline-flex;align-items:center;justify-content:center;border-radius:10px;border:1px solid var(--line);background:var(--control);color:var(--ink);padding:9px 14px;font-weight:700;text-decoration:none;cursor:pointer}.collections-page a.button-link:hover{border-color:#776953;background:#40372b}.collections-page a.button-link:focus-visible{outline:3px solid #d4a54a66;outline-offset:2px}.collections-page a.button-link.primary{background:var(--gold);border-color:var(--gold);color:#19150e}.collections-top-actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}.scope-note{font-size:.78rem;color:var(--gold-2)!important;max-width:720px}.backup-card{padding:16px;border:1px solid var(--line);border-radius:13px;background:#191611;margin-bottom:14px}.backup-card h4{font:1.12rem Georgia,serif;margin:0 0 6px}.backup-card p{color:var(--muted);line-height:1.5;margin:0 0 13px}.backup-summary{min-height:22px;color:var(--gold-2);margin-top:10px}.backup-fineprint{font-size:.76rem;color:var(--quiet)!important;margin-top:10px!important}
  .collections-app{background:var(--bg);border:1px solid #2e2921;border-radius:18px;min-height:72vh;overflow:hidden}.collections-top{display:flex;align-items:center;justify-content:space-between;gap:18px;padding:22px 24px;border-bottom:1px solid var(--line);background:linear-gradient(135deg,#1b1813,#211b12)}.collections-kicker,.section-kicker{font-size:.7rem;letter-spacing:.18em;text-transform:uppercase;color:var(--gold);font-weight:800}.collections-top h2,.collection-copy h2{font-family:Georgia,serif;font-weight:500;letter-spacing:-.025em;margin:3px 0 0}.collections-top h2{font-size:clamp(1.65rem,3vw,2.35rem)}.collections-top p,.muted{color:var(--muted);margin:5px 0 0}.collections-grid{display:grid;grid-template-columns:minmax(250px,29%) 1fr;min-height:650px}.collection-sidebar{border-right:1px solid var(--line);padding:14px;min-width:0;background:#181510}.sidebar-search{margin-bottom:10px}.collection-list{display:grid;align-content:start;gap:7px}.collection-button{width:100%;display:grid;grid-template-columns:46px 1fr;gap:11px;text-align:left;background:transparent!important;border-color:transparent!important;padding:9px!important}.collection-button:hover,.collection-button.active{background:#2b261e!important;border-color:#3e372c!important}.collection-thumb{width:46px;height:46px;border-radius:9px;background:linear-gradient(135deg,#795f2d,#242018);display:grid;place-items:center;color:#f5d891;font-family:Georgia,serif;font-size:1.15rem;overflow:hidden}.collection-button strong,.collection-button span{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.collection-button span{color:var(--muted);font-size:.77rem;margin-top:4px}.collection-detail{padding:clamp(20px,3.8vw,44px);min-width:0}.collection-empty{min-height:480px;display:grid;place-items:center;text-align:center;color:var(--muted)}
  .collection-hero{display:grid;grid-template-columns:148px 1fr;gap:24px;align-items:center}.collection-mosaic{width:148px;height:148px;border-radius:16px;overflow:hidden;background:linear-gradient(135deg,#765d2e,#211d16);display:grid;grid-template-columns:1fr 1fr;box-shadow:0 18px 50px #0006}.collection-mosaic:has(img:nth-child(3)){grid-template-rows:1fr 1fr}.collection-mosaic img{width:100%;height:100%;object-fit:cover;min-width:0;min-height:0}.collection-mosaic img:only-child{grid-column:1/-1}.collection-mosaic .mosaic-fallback{grid-column:1/-1;display:grid;place-items:center;color:#f5d891;font:2.2rem Georgia,serif}.collection-copy{min-width:0}.collection-copy h2{font-size:clamp(2rem,5vw,3.7rem);line-height:.98}.collection-description{max-width:700px;color:var(--muted);line-height:1.55}.collection-stats{display:flex;gap:18px;flex-wrap:wrap;margin:17px 0}.collection-stat strong{display:block;color:var(--gold);font:1.3rem Georgia,serif}.collection-stat span{color:var(--quiet);font-size:.68rem;letter-spacing:.14em;text-transform:uppercase}.toolbar{display:flex;gap:8px;flex-wrap:wrap}.manage-bar{display:grid;grid-template-columns:minmax(180px,1fr) 140px auto auto;gap:8px;align-items:center;margin-top:34px;padding:12px;background:#1b1813;border:1px solid #302a22;border-radius:13px}.collection-section{margin-top:28px}.section-heading{display:flex;align-items:baseline;justify-content:space-between;gap:12px;padding-bottom:10px;border-bottom:1px solid var(--line)}.section-heading h3{font:1.35rem Georgia,serif;margin:0}.section-heading span{color:var(--muted);font-size:.78rem}.media-row{display:grid;grid-template-columns:auto 48px minmax(0,1fr) auto auto;gap:12px;align-items:center;padding:10px 5px;border-bottom:1px solid #2c271f}.media-check{width:18px!important;height:18px;accent-color:var(--gold)}.media-art{width:48px;height:48px;min-height:48px!important;padding:0!important;border:0!important;border-radius:8px;background:linear-gradient(135deg,#67512a,#252018);overflow:hidden;display:grid;place-items:center;color:var(--gold);font-family:Georgia,serif}.media-row>.media-art:disabled{opacity:1;cursor:default}.media-art.round{border-radius:50%}.media-art img{width:100%;height:100%;object-fit:cover}.media-copy{min-width:0}.media-title{color:var(--ink);font-weight:750;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.media-sub{color:var(--muted);font-size:.79rem;margin-top:4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.media-actions{display:flex;gap:5px;align-items:center}.media-actions button{min-width:42px;padding:7px 9px;font-size:.76rem}.position-controls{display:flex;gap:4px}.empty{color:var(--muted);padding:34px 10px;text-align:center}.mobile-back{display:none}
  .collections-page dialog{width:min(780px,calc(100vw - 24px));max-height:min(88vh,900px);overflow:auto;border:1px solid var(--line);border-radius:18px;background:var(--panel);color:var(--ink);padding:0;box-shadow:0 28px 100px #000d}.collections-page dialog::backdrop{background:rgba(0,0,0,.76);backdrop-filter:blur(3px)}.dialog-head,.dialog-foot{position:sticky;z-index:3;display:flex;align-items:center;justify-content:space-between;gap:12px;padding:16px 18px;background:#211e18f5;border-bottom:1px solid var(--line)}.dialog-head{top:0}.dialog-foot{bottom:0;border:0;border-top:1px solid var(--line)}.dialog-title{font:1.3rem Georgia,serif;margin:0}.dialog-body{padding:18px;background:var(--panel)}.dialog-body label{display:grid;gap:6px;margin-bottom:14px;color:var(--ink)}
  #library-dialog{width:min(1180px,calc(100vw - 24px));height:min(91vh,960px);max-height:91vh;overflow:hidden}.library-shell{height:100%;display:grid;grid-template-rows:auto auto auto auto minmax(0,1fr) auto;background:var(--panel)}.library-head{position:static}.library-title-wrap{display:flex;align-items:center;gap:10px;min-width:0}.library-back[hidden]{display:none}.library-subtitle{color:var(--muted);font-size:.75rem;margin-top:2px}.library-search-row{display:grid;grid-template-columns:minmax(220px,1fr) 150px;gap:10px;padding:14px 18px 10px}.library-search{position:relative}.library-search input{padding-left:42px;padding-right:38px}.search-icon{position:absolute;left:14px;top:50%;transform:translateY(-50%);color:var(--gold)}.search-clear{position:absolute;right:6px;top:4px;min-height:34px!important;width:34px;padding:0!important;border:0!important;background:transparent!important}.scope-tabs{display:flex;gap:8px;overflow:auto;padding:2px 18px 12px}.scope-tabs button{white-space:nowrap;border-radius:999px;min-height:36px;padding:7px 14px}.scope-tabs button.active{background:var(--gold);border-color:var(--gold);color:#19150e}.browser-meta{display:flex;align-items:center;justify-content:space-between;gap:10px;min-height:34px;padding:4px 20px 10px;color:var(--muted);font-size:.8rem}.browser-status.error{color:var(--danger)}.browser-results{overflow:auto;padding:0 18px 24px;min-height:0}.browser-section{margin-top:16px}.browser-section:first-child{margin-top:4px}.browser-section-head{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:12px}.browser-section-head h3{font:1.35rem Georgia,serif;margin:0}.browser-section-head button{min-height:34px;padding:6px 10px;font-size:.75rem}.album-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(154px,1fr));gap:18px 14px}.album-card{position:relative;min-width:0}.album-open{width:100%;display:block;text-align:left;background:transparent!important;border:0!important;padding:0!important}.album-art{position:relative;width:100%;aspect-ratio:1;border-radius:13px;overflow:hidden;background:linear-gradient(135deg,#745b2c,#211d16);box-shadow:0 10px 28px #0005;display:grid}.album-art img,.album-art-fallback{grid-area:1/1}.album-art img{position:relative;z-index:1;width:100%;height:100%;object-fit:cover}.album-art-fallback{width:100%;height:100%;display:grid;place-items:center;color:#efd084;font:2rem Georgia,serif}.album-title{font-family:Georgia,serif;font-size:1.02rem;margin-top:9px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.album-meta{color:var(--muted);font-size:.74rem;margin-top:4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.select-media{width:100%;margin-top:7px;min-height:36px!important;padding:6px 9px!important;font-size:.76rem}.select-media.selected{background:#4b3b1f;border-color:var(--gold);color:var(--gold-2)}.select-media.member{color:#9fa891}.track-list{display:grid}.browser-track{display:grid;grid-template-columns:38px 48px minmax(0,1fr) auto auto;gap:11px;align-items:center;padding:9px 3px;border-bottom:1px solid #302a22}.preview-button{min-width:36px!important;width:36px;min-height:36px!important;height:36px;border-radius:50%!important;padding:0!important;display:grid;place-items:center}.preview-button.playing{background:var(--gold)!important;color:#19150e!important}.track-number{font-variant-numeric:tabular-nums;color:var(--quiet);font-size:.76rem;text-align:right}.artist-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(210px,1fr));gap:10px}.artist-card{display:grid;grid-template-columns:58px 1fr auto;gap:12px;align-items:center;text-align:left;padding:10px!important;background:#28241d!important}.artist-art{width:58px;height:58px;border-radius:50%;overflow:hidden;background:radial-gradient(circle at 35% 30%,#887042,#282118);display:grid;place-items:center;color:#efd084;font:1.2rem Georgia,serif}.artist-art img{width:100%;height:100%;object-fit:cover}.artist-card strong,.artist-card span{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.artist-card span{color:var(--muted);font-size:.73rem;margin-top:4px}.album-detail-hero{display:grid;grid-template-columns:180px 1fr;gap:24px;align-items:end;padding:12px 2px 24px}.album-detail-art{width:180px;height:180px;border-radius:15px;overflow:hidden;background:linear-gradient(135deg,#745b2c,#211d16);box-shadow:0 16px 40px #0008}.album-detail-art img{width:100%;height:100%;object-fit:cover}.album-detail-copy h3{font:clamp(2rem,5vw,3.6rem)/.98 Georgia,serif;margin:5px 0}.album-detail-copy p{color:var(--muted);margin:7px 0}.disc-heading{margin:20px 0 5px;color:var(--gold);font-size:.73rem;letter-spacing:.16em;text-transform:uppercase}.load-more{display:block;margin:22px auto}.selection-summary{display:flex;align-items:center;gap:10px}.selection-summary strong{color:var(--gold)}
  .toast{position:fixed;z-index:100;left:50%;bottom:92px;transform:translateX(-50%);width:min(480px,calc(100vw - 28px));display:flex;align-items:center;justify-content:space-between;gap:12px;background:#30291f;border:1px solid #675944;border-radius:12px;padding:12px 14px;box-shadow:0 16px 45px #000a}.toast[hidden]{display:none}.preview-player{position:fixed;z-index:90;left:50%;bottom:18px;transform:translateX(-50%);width:min(940px,calc(100vw - 24px));display:grid;grid-template-columns:48px minmax(150px,1fr) minmax(180px,2fr) auto;gap:12px;align-items:center;padding:9px 12px;background:#171a20f5;border:1px solid #3c414b;border-radius:14px;box-shadow:0 15px 55px #000c;backdrop-filter:blur(12px)}.preview-player[hidden]{display:none}.player-art{width:48px;height:48px;border-radius:8px;overflow:hidden;background:#303641;display:grid;place-items:center;color:var(--gold)}.player-art img{width:100%;height:100%;object-fit:cover}.player-copy{min-width:0}.player-copy strong,.player-copy span{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.player-copy span{color:#9da4b1;font-size:.75rem;margin-top:3px}.player-progress{display:grid;grid-template-columns:38px 1fr 38px;gap:8px;align-items:center;color:#939aa7;font-size:.7rem;font-variant-numeric:tabular-nums}.player-progress input{padding:0;border:0;accent-color:var(--gold);background:transparent}.player-controls{display:flex;gap:5px}.player-controls button{min-width:38px;padding:7px}.player-controls .player-main{border-radius:50%;background:var(--gold);color:#19150e}
  @media(max-width:760px){.collections-top{align-items:flex-start;flex-direction:column}.collections-top-actions{width:100%;justify-content:stretch}.collections-top-actions button{flex:1}.collections-grid{display:block}.collection-sidebar{border:0}.collection-detail{display:none}.collections-app.detail-open .collection-sidebar{display:none}.collections-app.detail-open .collection-detail{display:block}.mobile-back{display:inline-flex}.collection-hero{grid-template-columns:92px 1fr;gap:15px}.collection-mosaic{width:92px;height:92px}.collection-copy h2{font-size:2rem}.manage-bar{grid-template-columns:1fr 120px}.manage-bar button{width:100%}.media-row{grid-template-columns:auto 44px minmax(0,1fr) auto}.position-controls{display:none}.media-actions{grid-column:4}.library-search-row{grid-template-columns:1fr}.album-grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:15px 10px}.artist-grid{grid-template-columns:1fr}.browser-track{grid-template-columns:34px 44px minmax(0,1fr) auto}.browser-track .track-number{display:none}.album-detail-hero{grid-template-columns:100px 1fr;gap:14px;align-items:center}.album-detail-art{width:100px;height:100px}.album-detail-copy h3{font-size:1.8rem}.preview-player{bottom:8px;grid-template-columns:42px 1fr auto}.player-art{width:42px;height:42px}.player-progress{display:none}.player-controls button:nth-child(1),.player-controls button:nth-child(3){display:none}}
</style>
<div class="collections-page">
  <section class="collections-app" id="collections-app">
    <header class="collections-top">
      <div><span class="collections-kicker">AudioMuse library · __PRINCIPAL__</span><h2>Living Collections</h2><p>Albums stay albums. Tracks stay tracks. Both can seed a Lumae Vibe.</p><p class="scope-note">__PRINCIPAL_DETAIL__</p></div>
      <div class="collections-top-actions"><button type="button" id="open-backup">Backup &amp; restore</button><button type="button" class="primary" id="new-collection">New collection</button></div>
    </header>
    <div class="collections-grid">
      <aside class="collection-sidebar">
        <input class="sidebar-search" id="collection-filter" type="search" placeholder="Filter collections" aria-label="Filter collections">
        <nav class="collection-list" id="collection-list" aria-label="Collections"><p class="empty">Loading collections…</p></nav>
      </aside>
      <main class="collection-detail" id="collection-detail"><div class="collection-empty"><p>Choose a collection, or make a new one.</p></div></main>
    </div>
  </section>

  <dialog id="edit-dialog" aria-labelledby="edit-title">
    <form method="dialog" id="edit-form">
      <header class="dialog-head"><h3 class="dialog-title" id="edit-title" tabindex="-1">New collection</h3><button type="button" data-close-edit aria-label="Close">×</button></header>
      <div class="dialog-body"><label>Name<input id="collection-name" maxlength="120" required></label><label>Description<textarea id="collection-description" maxlength="1000" rows="4" placeholder="What belongs together, and why?"></textarea></label><p class="browser-status" id="edit-status" role="status"></p></div>
      <footer class="dialog-foot"><button type="button" class="ghost" data-close-edit>Cancel</button><button type="submit" class="primary" id="save-collection">Save collection</button></footer>
    </form>
  </dialog>

  <dialog id="backup-dialog" aria-labelledby="backup-title">
    <form id="backup-form">
      <header class="dialog-head"><h3 class="dialog-title" id="backup-title" tabindex="-1">Backup &amp; restore</h3><button type="button" data-close-backup aria-label="Close">×</button></header>
      <div class="dialog-body">
        <section class="backup-card"><h4>Back up all collections</h4><p>Downloads a versioned JSON file for this account only. It contains collection names, notes, ordering, and media references—never passwords, tokens, cover images, or audio. Sync pending Lumae edits first so AudioMuse can include them.</p><a class="button-link primary" href="api/collections/backup" download>Download full backup</a></section>
        <section class="backup-card"><h4>Restore from backup</h4><p>Choose a Lumae collection backup. Restore always creates new copies; it never replaces or deletes collections already here.</p><input id="backup-file" type="file" accept="application/json,.json" aria-label="Choose collection backup"><p class="backup-summary" id="backup-summary" role="status">No backup selected.</p><p class="backup-fineprint">Media is matched by its saved server IDs and album keys, so backups work best with the same media library.</p></section>
        <p class="browser-status" id="backup-status" role="alert"></p>
      </div>
      <footer class="dialog-foot"><button type="button" class="ghost" data-close-backup>Cancel</button><button type="submit" class="primary" id="restore-backup" disabled>Restore copies</button></footer>
    </form>
  </dialog>

  <dialog id="library-dialog" aria-labelledby="library-title">
    <section class="library-shell">
      <header class="dialog-head library-head">
        <div class="library-title-wrap"><button type="button" class="library-back ghost" id="library-back" hidden aria-label="Back">←</button><div><h3 class="dialog-title" id="library-title" tabindex="-1">Add from your library</h3><div class="library-subtitle" id="library-subtitle">Browse analyzed music</div></div></div>
        <button type="button" id="close-library" aria-label="Close library browser">×</button>
      </header>
      <div class="library-search-row"><div class="library-search"><span class="search-icon">⌕</span><input type="search" id="library-search" placeholder="Search albums, tracks, and artists" autocomplete="off" aria-label="Search the music library"><button type="button" class="search-clear" id="clear-library-search" aria-label="Clear search">×</button></div><select id="library-sort" aria-label="Sort library"><option value="title">Sort: title</option><option value="artist">Sort: artist</option><option value="year">Sort: newest year</option></select></div>
      <nav class="scope-tabs" id="scope-tabs" aria-label="Library categories"><button type="button" data-scope="all" class="active">All</button><button type="button" data-scope="albums">Albums</button><button type="button" data-scope="tracks">Tracks</button><button type="button" data-scope="artists">Artists</button></nav>
      <div class="browser-meta"><span id="browser-count">Browse the full analyzed library</span><span class="browser-status" id="browser-status" role="status" aria-live="polite"></span></div>
      <div class="browser-results" id="browser-results"><p class="empty">Loading your library…</p></div>
      <footer class="dialog-foot"><div class="selection-summary"><strong id="selected-count">0 selected</strong><button type="button" class="ghost" id="clear-selection" disabled>Clear</button></div><button type="button" class="primary" id="add-selected" disabled>Add selected</button></footer>
    </section>
  </dialog>

  <div class="toast" id="collection-toast" role="status" hidden><span id="toast-label"></span><button type="button" id="toast-action" hidden></button></div>
  <section class="preview-player" id="preview-player" aria-label="Audio preview player" hidden>
    <audio id="preview-audio" preload="metadata"></audio><div class="player-art" id="player-art">♪</div><div class="player-copy"><strong id="player-title">Not playing</strong><span id="player-artist"></span></div><div class="player-progress"><span id="player-current">0:00</span><input id="player-seek" type="range" min="0" max="1000" value="0" aria-label="Preview position"><span id="player-duration">0:00</span></div><div class="player-controls"><button type="button" id="player-back" aria-label="Back 10 seconds">↶</button><button type="button" class="player-main" id="player-toggle" aria-label="Play or pause">▶</button><button type="button" id="player-forward" aria-label="Forward 10 seconds">↷</button><button type="button" id="player-close" aria-label="Stop and close preview">×</button></div>
  </section>
</div>
<script>
(()=>{
'use strict';
const api='api/collections';
const app=document.getElementById('collections-app'),list=document.getElementById('collection-list'),detail=document.getElementById('collection-detail');
const edit=document.getElementById('edit-dialog'),backup=document.getElementById('backup-dialog'),library=document.getElementById('library-dialog'),browserResults=document.getElementById('browser-results'),browserStatus=document.getElementById('browser-status');
const audio=document.getElementById('preview-audio'),player=document.getElementById('preview-player');
let collections=[],current=null,items=[],editMode='new',checkedItems=new Set(),collectionQuery='',collectionKind='all',itemQuery='',undoItem=null,toastTimer=null;
let selected=new Map(),browserLookup=[],searchTimer=null;
let restoreDocument=null;
let browser={scope:'all',query:'',artist:null,sort:'title',page:1,limit:36,data:null,controller:null,generation:0,albumDetail:null,stack:[]};
let preview={id:null,item:null};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const initials=s=>String(s||'?').split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase();
const mutationKey=()=>globalThis.crypto?.randomUUID?.()||`${Date.now()}-${Math.random().toString(16).slice(2)}`;
const artUrl=(id,size=320)=>id?`${api}/library/art/${encodeURIComponent(id)}?size=${size}`:'';
const streamUrl=id=>`${api}/library/stream/${encodeURIComponent(id)}`;
const formatTime=value=>{if(!Number.isFinite(value)||value<0)return'0:00';const m=Math.floor(value/60),s=Math.floor(value%60);return`${m}:${String(s).padStart(2,'0')}`};
async function req(url,opts={}){const headers=opts.headers||{},rest={...opts};delete rest.headers;const r=await fetch(url,{...rest,headers:{'Content-Type':'application/json',...headers}});let body;try{body=await r.json()}catch(_){body={error:`Request failed (${r.status})`}}if(!r.ok)throw Object.assign(new Error(body.error||'Request failed'),{status:r.status,body});return body}
function wireArt(root=document){root.querySelectorAll('img[data-art]').forEach(img=>{img.onerror=()=>img.remove()})}
function toast(message,actionLabel='',action=null){clearTimeout(toastTimer);document.getElementById('toast-label').textContent=message;const button=document.getElementById('toast-action');button.hidden=!actionLabel;button.textContent=actionLabel;button.onclick=action?()=>runAction(action):null;document.getElementById('collection-toast').hidden=false;toastTimer=setTimeout(()=>document.getElementById('collection-toast').hidden=true,6000)}
function runAction(action){Promise.resolve().then(action).catch(error=>toast(error.status===409?'This collection changed elsewhere. Reload it and try again.':error.message||'The action could not be completed.'))}
function collectionMembership(){const keys=new Set();for(const item of items){if(item.kind==='track')keys.add(`track:${item.track_id}`);else{if(item.album_key)keys.add(`album-key:${item.album_key.toLowerCase()}`);if(item.provider_album_id)keys.add(`album-id:${item.provider_album_id}`)}}return keys}
function itemKeys(item){if(item.kind==='track')return[`track:${item.track_id}`];return[`album-key:${String(item.album_key||`${item.artist||''}::${item.title||''}`).toLowerCase()}`,item.provider_album_id?`album-id:${item.provider_album_id}`:''].filter(Boolean)}
function selectionKey(item){return itemKeys(item)[0]}
function isMember(item){const members=collectionMembership();return itemKeys(item).some(key=>members.has(key))}
function itemPayload(item){return item.kind==='track'?{kind:'track',track_id:item.track_id,title:item.title,artist:item.artist,album:item.album,cover_item_id:item.cover_item_id}:{kind:'album',provider_album_id:item.provider_album_id||null,album_key:item.album_key||`${String(item.artist||'').toLowerCase()}::${String(item.title||'').toLowerCase()}`,title:item.title,artist:item.artist,cover_item_id:item.cover_item_id}}

async function refreshCollections(selectId=null){const body=await req(api);collections=body.collections||[];renderCollectionList();if(selectId)await loadCollection(selectId);else if(!current&&collections.length)await loadCollection(collections[0].id);else if(!collections.length){current=null;items=[];renderDetail()}}
function renderCollectionList(){const visible=collections.filter(c=>!collectionQuery||`${c.name} ${c.description||''}`.toLowerCase().includes(collectionQuery));list.innerHTML=visible.length?visible.map(c=>`<button type="button" class="collection-button ${current?.id===c.id?'active':''}" data-collection-id="${esc(c.id)}"><span class="collection-thumb">${esc(initials(c.name))}</span><span><strong>${esc(c.name)}</strong><span>${c.album_count} albums · ${c.track_count} tracks</span></span></button>`).join(''):'<p class="empty">No matching collections.</p>';list.querySelectorAll('[data-collection-id]').forEach(button=>button.onclick=()=>runAction(()=>loadCollection(button.dataset.collectionId)))}
async function loadCollection(id){const body=await req(`${api}/${encodeURIComponent(id)}`),changed=current?.id!==body.collection.id;current=body.collection;items=body.items||[];if(changed){itemQuery='';collectionKind='all'}const summaryIndex=collections.findIndex(collection=>collection.id===current.id);if(summaryIndex>=0)collections[summaryIndex]={...collections[summaryIndex],...current};checkedItems.clear();app.classList.add('detail-open');renderCollectionList();renderDetail()}
function collectionMosaic(){const covers=[...new Set(items.map(i=>i.cover_item_id||i.track_id).filter(Boolean))].slice(0,4);if(!covers.length)return`<div class="mosaic-fallback">${esc(initials(current.name))}</div>`;return covers.map(id=>`<img data-art src="${esc(artUrl(id,240))}" alt="">`).join('')}
function filteredItems(){return items.filter(item=>(collectionKind==='all'||item.kind===collectionKind)&&(!itemQuery||`${item.title||''} ${item.artist||''} ${item.album||''}`.toLowerCase().includes(itemQuery)))}
function renderItemRows(kind,title,rows){return`<section class="collection-section"><div class="section-heading"><h3>${title}</h3><span>${rows.length}</span></div>${rows.length?rows.map(item=>{const index=items.findIndex(candidate=>candidate.id===item.id),kindRows=items.filter(candidate=>candidate.kind===kind),kindIndex=kindRows.findIndex(candidate=>candidate.id===item.id),cover=item.cover_item_id||item.track_id;return`<div class="media-row"><input class="media-check" type="checkbox" data-check-item="${esc(item.id)}" ${checkedItems.has(item.id)?'checked':''} aria-label="Select ${esc(item.title||'item')}"><button type="button" class="media-art" data-open-collection-album="${index}" ${kind==='track'?'disabled':''}>${cover?`<img data-art src="${esc(artUrl(cover,120))}" alt="">`:esc(initials(item.title))}</button><div class="media-copy"><div class="media-title">${esc(item.title||'Untitled')}</div><div class="media-sub">${esc(item.artist||'Unknown artist')}${item.album?` · ${esc(item.album)}`:''}</div></div><div class="media-actions">${kind==='track'?`<button type="button" class="preview-button" data-preview-index="c${index}" aria-label="Preview ${esc(item.title)}">▶</button>`:`<button type="button" data-open-collection-album="${index}">View album</button>`}<button type="button" data-remove-item="${esc(item.id)}">Remove</button></div><div class="position-controls"><button type="button" data-move-item="${esc(item.id)}" data-direction="-1" ${kindIndex===0?'disabled':''} aria-label="Move up">↑</button><button type="button" data-move-item="${esc(item.id)}" data-direction="1" ${kindIndex===kindRows.length-1?'disabled':''} aria-label="Move down">↓</button></div></div>`}).join(''):'<p class="empty">Nothing here yet.</p>'}</section>`}
function renderDetail(){if(!current){detail.innerHTML='<div class="collection-empty"><p>Create your first collection to start gathering albums and tracks.</p></div>';return}const visible=filteredItems(),albums=visible.filter(i=>i.kind==='album'),tracks=visible.filter(i=>i.kind==='track');detail.innerHTML=`<button type="button" class="mobile-back" id="mobile-back">← Collections</button><section class="collection-hero"><div class="collection-mosaic">${collectionMosaic()}</div><div class="collection-copy"><span class="section-kicker">Mixed media collection</span><h2>${esc(current.name)}</h2><p class="collection-description">${esc(current.description||'Albums and individual tracks, kept together without flattening them into a playlist.')}</p><div class="collection-stats"><div class="collection-stat"><strong>${current.album_count}</strong><span>albums</span></div><div class="collection-stat"><strong>${current.track_count}</strong><span>tracks</span></div><div class="collection-stat"><strong>${current.revision}</strong><span>revision</span></div></div><div class="toolbar"><button type="button" class="primary" id="add-music">Browse library</button><button type="button" id="edit-collection">Edit</button><button type="button" id="duplicate-collection">Duplicate</button><a class="button-link" href="${api}/${encodeURIComponent(current.id)}/export" download>Export</a><button type="button" class="danger" id="delete-collection">Delete</button></div></div></section><div class="manage-bar"><input id="item-filter" type="search" value="${esc(itemQuery)}" placeholder="Filter this collection" aria-label="Filter collection items"><select id="item-kind" aria-label="Collection item type"><option value="all">Albums + tracks</option><option value="album">Albums</option><option value="track">Tracks</option></select><button type="button" id="select-visible">Select visible</button><button type="button" class="danger" id="remove-selected" ${checkedItems.size?'':'disabled'}>Remove selected (${checkedItems.size})</button></div>${renderItemRows('album','Albums',albums)}${renderItemRows('track','Individual tracks',tracks)}`;document.getElementById('item-kind').value=collectionKind;wireArt(detail);wireDetail()}
function wireDetail(){document.getElementById('mobile-back').onclick=()=>app.classList.remove('detail-open');document.getElementById('add-music').onclick=openLibrary;document.getElementById('edit-collection').onclick=()=>openEdit('edit');document.getElementById('duplicate-collection').onclick=()=>runAction(duplicateCollection);document.getElementById('delete-collection').onclick=()=>runAction(deleteCollection);document.getElementById('item-filter').oninput=event=>{itemQuery=event.target.value.trim().toLowerCase();renderDetail();const field=document.getElementById('item-filter');field.focus();field.setSelectionRange(field.value.length,field.value.length)};document.getElementById('item-kind').onchange=e=>{collectionKind=e.target.value;renderDetail()};document.getElementById('select-visible').onclick=()=>{filteredItems().forEach(i=>checkedItems.add(i.id));renderDetail()};document.getElementById('remove-selected').onclick=()=>runAction(removeSelected);detail.querySelectorAll('[data-check-item]').forEach(box=>box.onchange=()=>{box.checked?checkedItems.add(box.dataset.checkItem):checkedItems.delete(box.dataset.checkItem);renderDetail()});detail.querySelectorAll('[data-remove-item]').forEach(button=>button.onclick=()=>runAction(()=>removeItem(button.dataset.removeItem)));detail.querySelectorAll('[data-move-item]').forEach(button=>button.onclick=()=>runAction(()=>moveItem(button.dataset.moveItem,Number(button.dataset.direction))));detail.querySelectorAll('[data-open-collection-album]').forEach(button=>button.onclick=()=>{const item=items[Number(button.dataset.openCollectionAlbum)];if(item?.kind==='album')openLibraryAlbum(item)});detail.querySelectorAll('[data-preview-index]').forEach(button=>button.onclick=()=>{const item=items[Number(button.dataset.previewIndex.slice(1))];if(item)playPreview(item)});updatePreviewIndicators()}

function openEdit(mode){editMode=mode;document.getElementById('edit-title').textContent=mode==='new'?'New collection':'Edit collection';document.getElementById('collection-name').value=mode==='edit'?(current?.name||''):'';document.getElementById('collection-description').value=mode==='edit'?(current?.description||''):'';document.getElementById('edit-status').textContent='';edit.showModal();setTimeout(()=>document.getElementById('collection-name').focus(),0)}
function resetBackup(){restoreDocument=null;document.getElementById('backup-file').value='';document.getElementById('backup-summary').textContent='No backup selected.';document.getElementById('backup-status').textContent='';document.getElementById('backup-status').classList.remove('error');document.getElementById('restore-backup').disabled=true}
function openBackup(){resetBackup();backup.showModal();setTimeout(()=>document.getElementById('backup-title').focus(),0)}
async function inspectBackup(event){const file=event.target.files?.[0],summary=document.getElementById('backup-summary'),status=document.getElementById('backup-status');restoreDocument=null;summary.textContent='No backup selected.';status.textContent='';status.classList.remove('error');document.getElementById('restore-backup').disabled=true;if(!file)return;if(file.size>50*1024*1024){status.textContent='This backup is larger than the 50 MB restore limit.';status.classList.add('error');return}try{const documentBody=JSON.parse(await file.text());if(documentBody?.format!=='lumae-living-collections'||documentBody?.version!==1||!Array.isArray(documentBody?.collections))throw new Error('This is not a supported Lumae Living Collections backup.');const collectionCount=documentBody.collections.length,itemCount=documentBody.collections.reduce((sum,collection)=>sum+(Array.isArray(collection?.items)?collection.items.length:0),0);if(!collectionCount)throw new Error('This backup does not contain any collections.');restoreDocument=documentBody;summary.textContent=`Ready to add ${collectionCount} ${collectionCount===1?'collection':'collections'} with ${itemCount} ${itemCount===1?'item':'items'} as new copies.`;document.getElementById('restore-backup').disabled=false}catch(error){status.textContent=error.message||'The selected backup could not be read.';status.classList.add('error')}}
async function restoreBackup(event){event.preventDefault();if(!restoreDocument)return;const button=document.getElementById('restore-backup'),status=document.getElementById('backup-status');button.disabled=true;button.textContent='Restoring…';status.textContent='Validating and restoring your collections…';status.classList.remove('error');let body;try{body=await req(`${api}/restore`,{method:'POST',headers:{'Idempotency-Key':mutationKey()},body:JSON.stringify(restoreDocument)})}catch(error){status.textContent=error.message||'The backup could not be restored.';status.classList.add('error');button.textContent='Restore copies';button.disabled=false;return}backup.close();button.textContent='Restore copies';button.disabled=false;try{await refreshCollections(body.collections?.[0]?.id||null);toast(`Restored ${body.collection_count} ${body.collection_count===1?'collection':'collections'} as new copies.`)}catch(_){toast('Collections were restored. Reload this page to refresh the list.')}}
async function saveCollection(event){event.preventDefault();const payload={name:document.getElementById('collection-name').value,description:document.getElementById('collection-description').value};const status=document.getElementById('edit-status');status.textContent='Saving…';try{const body=editMode==='new'?await req(api,{method:'POST',headers:{'Idempotency-Key':mutationKey()},body:JSON.stringify(payload)}):await req(`${api}/${current.id}`,{method:'PATCH',headers:{'If-Match':String(current.revision),'Idempotency-Key':mutationKey()},body:JSON.stringify(payload)});edit.close();await refreshCollections(body.collection.id);toast(editMode==='new'?'Collection created.':'Collection updated.')}catch(error){status.textContent=error.status===409?'This collection changed elsewhere. Reload it and try again.':error.message;status.classList.add('error')}}
async function duplicateCollection(){const created=await req(api,{method:'POST',headers:{'Idempotency-Key':mutationKey()},body:JSON.stringify({name:`${current.name} copy`,description:current.description})});const copies=items.map(({id,collection_id,added_at,updated_at,...item})=>item);if(copies.length)await req(`${api}/${created.collection.id}/items/batch`,{method:'POST',headers:{'If-Match':'1','Idempotency-Key':mutationKey()},body:JSON.stringify({items:copies})});await refreshCollections(created.collection.id);toast('Collection duplicated.')}
async function deleteCollection(){if(!confirm(`Delete “${current.name}”? The music itself will not be deleted.`))return;await req(`${api}/${current.id}`,{method:'DELETE',headers:{'If-Match':String(current.revision),'Idempotency-Key':mutationKey()}});current=null;items=[];app.classList.remove('detail-open');await refreshCollections();toast('Collection deleted.')}
async function moveItem(id,direction){const moving=items.find(i=>i.id===id),rows=items.filter(i=>i.kind===moving?.kind),index=rows.findIndex(i=>i.id===id),target=index+direction;if(!moving||target<0||target>=rows.length)return;[rows[index],rows[target]]=[rows[target],rows[index]];await req(`${api}/${current.id}/items/batch`,{method:'POST',headers:{'If-Match':String(current.revision),'Idempotency-Key':mutationKey()},body:JSON.stringify({items:rows.map((item,position)=>({...item,position}))})});await loadCollection(current.id)}
async function removeItem(id){const removed=items.find(i=>i.id===id);await req(`${api}/${current.id}/items/${encodeURIComponent(id)}`,{method:'DELETE',headers:{'If-Match':String(current.revision),'Idempotency-Key':mutationKey()}});await loadCollection(current.id);if(removed){undoItem=removed;toast(`Removed ${removed.title||'item'}`,'Undo',undoRemove)}}
async function undoRemove(){if(!undoItem)return;const item=undoItem;undoItem=null;await req(`${api}/${current.id}/items/${encodeURIComponent(item.id)}`,{method:'PUT',headers:{'If-Match':String(current.revision),'Idempotency-Key':mutationKey()},body:JSON.stringify(item)});await loadCollection(current.id);toast(`${item.title||'Item'} restored.`)}
async function removeSelected(){const ids=[...checkedItems];if(!ids.length||!confirm(`Remove ${ids.length} selected ${ids.length===1?'item':'items'} from this collection?`))return;const body=await req(`${api}/${current.id}/items/batch`,{method:'DELETE',headers:{'If-Match':String(current.revision),'Idempotency-Key':mutationKey()},body:JSON.stringify({item_ids:ids})});checkedItems.clear();await loadCollection(current.id);toast(`Removed ${body.deleted_count} ${body.deleted_count===1?'item':'items'}.`)}

function stash(item){browserLookup.push(item);return browserLookup.length-1}
function selectionButton(item,index){const member=isMember(item),on=selected.has(selectionKey(item));return`<button type="button" class="select-media ${on?'selected':''} ${member?'member':''}" data-select-index="${index}" ${member?'disabled':''} aria-pressed="${on}">${member?'Already in collection':on?'Selected ✓':item.kind==='album'?'Select album':'Select track'}</button>`}
function albumCard(item){const index=stash(item),cover=item.cover_item_id;return`<article class="album-card"><button type="button" class="album-open" data-open-album="${index}" aria-label="Open ${esc(item.title)}"><div class="album-art">${cover?`<img data-art src="${esc(artUrl(cover,360))}" alt="">`:''}<div class="album-art-fallback">${esc(initials(item.title))}</div></div><div class="album-title">${esc(item.title)}</div><div class="album-meta">${esc(item.artist)}${item.year?` · ${item.year}`:''} · ${item.track_count||0} tracks</div></button>${selectionButton(item,index)}</article>`}
function trackRow(item,number=null){const index=stash(item),cover=item.cover_item_id||item.track_id,disabled=item.analyzed===false;return`<div class="browser-track"><span class="track-number">${number??''}</span><div class="media-art">${cover?`<img data-art src="${esc(artUrl(cover,120))}" alt="">`:esc(initials(item.title))}</div><div class="media-copy"><div class="media-title">${esc(item.title)}</div><div class="media-sub">${esc(item.artist)}${item.album?` · ${esc(item.album)}`:''}${disabled?' · not analyzed':''}</div></div><button type="button" class="preview-button" data-preview-index="b${index}" aria-label="Preview ${esc(item.title)}">▶</button>${disabled?'<button type="button" disabled title="Analyze this track before using it as a Vibe seed">Unavailable</button>':selectionButton(item,index)}</div>`}
function artistCard(item){const index=stash(item),cover=item.cover_item_id;return`<button type="button" class="artist-card" data-open-artist="${index}"><span class="artist-art">${cover?`<img data-art src="${esc(artUrl(cover,160))}" alt="">`:esc(initials(item.artist))}</span><span><strong>${esc(item.artist)}</strong><span>${item.album_count} albums · ${item.track_count} tracks</span></span><span>›</span></button>`}
function sectionHtml(label,key,section){if(!section?.items?.length)return`<section class="browser-section"><div class="browser-section-head"><h3>${label}</h3></div><p class="empty">No ${label.toLowerCase()} found.</p></section>`;const content=key==='albums'?`<div class="album-grid">${section.items.map(albumCard).join('')}</div>`:key==='tracks'?`<div class="track-list">${section.items.map(item=>trackRow(item)).join('')}</div>`:`<div class="artist-grid">${section.items.map(artistCard).join('')}</div>`;return`<section class="browser-section"><div class="browser-section-head"><h3>${label}</h3>${browser.scope==='all'&&section.total>section.items.length?`<button type="button" data-view-scope="${key}">View all ${section.total}</button>`:`<span class="muted">${section.total} total</span>`}</div>${content}</section>`}
function renderBrowser(){browserLookup=[];browser.albumDetail=null;const sections=browser.data?.sections||{};browserResults.innerHTML=[sectionHtml('Albums','albums',sections.albums),sectionHtml('Tracks','tracks',sections.tracks),sectionHtml('Artists','artists',sections.artists)].filter((_,index)=>browser.scope==='all'||['albums','tracks','artists'][index]===browser.scope).join('');const active=sections[browser.scope];document.getElementById('browser-count').textContent=browser.scope==='all'?'Top matches across albums, tracks, and artists':`${active?.total||0} ${browser.scope}${browser.artist?` by ${browser.artist}`:''}`;wireArt(browserResults);wireBrowserResults();updateSelection();updatePreviewIndicators()}
function wireBrowserResults(){browserResults.querySelectorAll('[data-select-index]').forEach(button=>button.onclick=()=>toggleSelection(browserLookup[Number(button.dataset.selectIndex)]));browserResults.querySelectorAll('[data-open-album]').forEach(button=>button.onclick=()=>openAlbum(browserLookup[Number(button.dataset.openAlbum)]));browserResults.querySelectorAll('[data-open-artist]').forEach(button=>button.onclick=()=>openArtist(browserLookup[Number(button.dataset.openArtist)]));browserResults.querySelectorAll('[data-preview-index]').forEach(button=>button.onclick=()=>playPreview(browserLookup[Number(button.dataset.previewIndex.slice(1))]));browserResults.querySelectorAll('[data-view-scope]').forEach(button=>button.onclick=()=>setScope(button.dataset.viewScope));browserResults.querySelectorAll('[data-load-more]').forEach(button=>button.onclick=()=>loadBrowser(true))}
async function loadBrowser(append=false){if(browser.query&&browser.query.length<3){browser.controller?.abort();browser.generation+=1;browserStatus.textContent='Type at least three characters to search.';browserStatus.classList.remove('error');document.getElementById('browser-count').textContent='Search waits for a specific term so the library stays responsive.';browserResults.innerHTML='<p class="empty">Keep typing to search albums, tracks, and artists.</p>';return}browser.controller?.abort();const controller=new AbortController();browser.controller=controller;const generation=++browser.generation;if(!append)browser.page=1;browserStatus.textContent='Loading…';browserStatus.classList.remove('error');try{const params=new URLSearchParams({scope:browser.scope,q:browser.query,sort:browser.sort,page:String(browser.page),limit:String(browser.limit)});if(browser.artist)params.set('artist',browser.artist);const body=await req(`${api}/library?${params}`,{signal:controller.signal});if(generation!==browser.generation)return;if(append&&browser.data){for(const [key,section]of Object.entries(body.sections||{})){const old=browser.data.sections[key];if(old)old.items=[...old.items,...section.items];else browser.data.sections[key]=section}browser.data.page=body.page}else browser.data=body;browserStatus.textContent='';renderBrowser();const section=browser.data.sections[browser.scope];if(browser.scope!=='all'&&section&&section.items.length<section.total){const more=document.createElement('button');more.type='button';more.className='load-more';more.dataset.loadMore='true';more.textContent=`Load more (${section.total-section.items.length} remaining)`;more.onclick=()=>{browser.page+=1;loadBrowser(true)};browserResults.appendChild(more)}}catch(error){if(error.name==='AbortError')return;browserStatus.textContent=error.message;browserStatus.classList.add('error');if(!append)browserResults.innerHTML='<p class="empty">The library could not be loaded. Try again.</p>'}finally{if(browser.controller===controller)browser.controller=null}}
function setScope(scope){browser.scope=scope;browser.page=1;document.querySelectorAll('[data-scope]').forEach(button=>button.classList.toggle('active',button.dataset.scope===scope));loadBrowser()}
function pushBrowserState(){browser.stack.push({scope:browser.scope,query:browser.query,artist:browser.artist,sort:browser.sort,page:browser.page,data:browser.data})}
function openArtist(item){pushBrowserState();browser.artist=item.artist;browser.scope='albums';browser.query='';browser.page=1;document.getElementById('library-search').value='';document.getElementById('library-title').textContent=item.artist;document.getElementById('library-subtitle').textContent='Albums and tracks by this artist';document.getElementById('library-back').hidden=false;document.querySelectorAll('[data-scope]').forEach(button=>button.classList.toggle('active',button.dataset.scope==='albums'));loadBrowser()}
async function openAlbum(item){pushBrowserState();browser.controller?.abort();browser.albumDetail={album:item,tracks:[],loading:true};document.getElementById('library-title').textContent=item.title;document.getElementById('library-subtitle').textContent=item.artist;document.getElementById('library-back').hidden=false;document.getElementById('scope-tabs').hidden=true;document.querySelector('.library-search-row').hidden=true;browserResults.innerHTML='<p class="empty">Loading live album metadata and track numbers from the media server…</p>';browserStatus.textContent='';try{const params=new URLSearchParams({title:item.title,artist:item.artist});if(item.provider_album_id)params.set('provider_album_id',item.provider_album_id);browser.albumDetail=await req(`${api}/library/album?${params}`);renderAlbumDetail()}catch(error){browserStatus.textContent=error.message;browserStatus.classList.add('error');browserResults.innerHTML='<p class="empty">Album details are unavailable.</p>'}}
function renderAlbumDetail(){browserLookup=[];const body=browser.albumDetail,album=body.album,tracks=body.tracks||[],cover=album.cover_item_id;let lastDisc=null;const rows=[];for(const track of tracks){const disc=track.disc_number||1;if(tracks.some(t=>(t.disc_number||1)!==disc)&&disc!==lastDisc)rows.push(`<div class="disc-heading">Disc ${disc}</div>`);lastDisc=disc;const number=track.track_number!=null?String(track.track_number).padStart(2,'0'):'—';rows.push(trackRow(track,number))}const albumIndex=stash(album);browserResults.innerHTML=`<section class="album-detail-hero"><div class="album-detail-art">${cover?`<img data-art src="${esc(artUrl(cover,480))}" alt="">`:''}</div><div class="album-detail-copy"><span class="section-kicker">Album · ${esc(body.metadata_source==='media_server'?'live media-server metadata':'analysis metadata')}</span><h3>${esc(album.title)}</h3><p>${esc(album.artist)}${album.year?` · ${album.year}`:''} · ${tracks.length} tracks</p>${selectionButton(album,albumIndex)}</div></section><div class="track-list">${rows.join('')||'<p class="empty">No tracks were found for this album.</p>'}</div>`;document.getElementById('browser-count').textContent=body.metadata_source==='media_server'?'Track and disc numbers loaded from your media server.':'Media-server metadata unavailable; showing analyzed tracks without track numbers.';wireArt(browserResults);wireBrowserResults();updateSelection();updatePreviewIndicators()}
function browserBack(){const state=browser.stack.pop();if(!state)return;browser.scope=state.scope;browser.query=state.query;browser.artist=state.artist;browser.sort=state.sort;browser.page=state.page;browser.data=state.data;browser.albumDetail=null;document.getElementById('scope-tabs').hidden=false;document.querySelector('.library-search-row').hidden=false;document.getElementById('library-search').value=browser.query;document.getElementById('library-title').textContent=browser.artist||'Add from your library';document.getElementById('library-subtitle').textContent=browser.artist?'Albums and tracks by this artist':'Browse analyzed music';document.getElementById('library-back').hidden=browser.stack.length===0&&!browser.artist;document.querySelectorAll('[data-scope]').forEach(button=>button.classList.toggle('active',button.dataset.scope===browser.scope));browser.data?renderBrowser():loadBrowser()}
function toggleSelection(item){if(!item||isMember(item))return;const key=selectionKey(item);selected.has(key)?selected.delete(key):selected.set(key,item);browser.albumDetail?renderAlbumDetail():renderBrowser()}
function updateSelection(){const count=selected.size;document.getElementById('selected-count').textContent=`${count} selected`;document.getElementById('clear-selection').disabled=!count;document.getElementById('add-selected').disabled=!count||!current;document.getElementById('add-selected').textContent=count?`Add ${count} selected`:'Add selected'}
async function addSelected(){if(!selected.size||!current)return;const button=document.getElementById('add-selected'),chosen=[...selected.values()].map(itemPayload),count=chosen.length;button.disabled=true;button.textContent='Adding…';browserStatus.textContent=`Adding ${count} ${count===1?'item':'items'}…`;try{await req(`${api}/${current.id}/items/batch`,{method:'POST',headers:{'If-Match':String(current.revision),'Idempotency-Key':mutationKey()},body:JSON.stringify({items:chosen})});selected.clear();await loadCollection(current.id);browserStatus.textContent=`Added ${count} ${count===1?'item':'items'} to ${current.name}.`;browser.albumDetail?renderAlbumDetail():renderBrowser();toast(`Added ${count} ${count===1?'item':'items'} to ${current.name}.`)}catch(error){browserStatus.textContent=error.status===409?'This collection changed elsewhere. Close and reopen the browser, then try again.':error.message;browserStatus.classList.add('error')}finally{updateSelection()}}
function openLibrary(){if(!current)return;selected.clear();browser={scope:'all',query:'',artist:null,sort:'title',page:1,limit:36,data:null,controller:null,generation:browser.generation+1,albumDetail:null,stack:[]};document.getElementById('library-search').value='';document.getElementById('library-sort').value='title';document.getElementById('library-title').textContent='Add from your library';document.getElementById('library-subtitle').textContent='Browse analyzed music';document.getElementById('library-back').hidden=true;document.getElementById('scope-tabs').hidden=false;document.querySelector('.library-search-row').hidden=false;document.querySelectorAll('[data-scope]').forEach(button=>button.classList.toggle('active',button.dataset.scope==='all'));library.showModal();updateSelection();loadBrowser();setTimeout(()=>document.getElementById('library-search').focus(),0)}
function openLibraryAlbum(item){openLibrary();setTimeout(()=>openAlbum(item),0)}
function closeLibrary(){clearTimeout(searchTimer);browser.controller?.abort();browser.generation+=1;library.close()}

function updatePreviewIndicators(){const playing=preview.id&&!audio.paused;document.querySelectorAll('[data-preview-index]').forEach(button=>{let item;if(button.dataset.previewIndex[0]==='b')item=browserLookup[Number(button.dataset.previewIndex.slice(1))];else item=items[Number(button.dataset.previewIndex.slice(1))];const active=playing&&item?.track_id===preview.id;button.classList.toggle('playing',active);button.textContent=active?'❚❚':'▶';button.setAttribute('aria-label',`${active?'Pause':'Preview'} ${item?.title||'track'}`)});document.getElementById('player-toggle').textContent=playing?'❚❚':'▶'}
function playPreview(item){if(!item?.track_id)return;player.hidden=false;if(preview.id===item.track_id){audio.paused?audio.play().catch(previewError):audio.pause();return}preview={id:item.track_id,item};document.getElementById('player-title').textContent=item.title||'Unknown track';document.getElementById('player-artist').textContent=[item.artist,item.album].filter(Boolean).join(' · ');const cover=item.cover_item_id||item.track_id;document.getElementById('player-art').innerHTML=cover?`<img data-art src="${esc(artUrl(cover,120))}" alt="">`:'♪';wireArt(document.getElementById('player-art'));audio.src=streamUrl(item.track_id);audio.play().catch(previewError);updatePreviewIndicators()}
function previewError(){document.getElementById('player-artist').textContent='Preview unavailable from the media server';updatePreviewIndicators()}
function stopPreview(){audio.pause();audio.removeAttribute('src');audio.load();preview={id:null,item:null};player.hidden=true;updatePreviewIndicators()}
function updatePlayerTime(){document.getElementById('player-current').textContent=formatTime(audio.currentTime);document.getElementById('player-duration').textContent=formatTime(audio.duration);document.getElementById('player-seek').value=Number.isFinite(audio.duration)&&audio.duration>0?String(Math.round(audio.currentTime/audio.duration*1000)):'0'}

document.getElementById('collection-filter').oninput=event=>{collectionQuery=event.target.value.trim().toLowerCase();renderCollectionList()};document.getElementById('new-collection').onclick=()=>openEdit('new');document.getElementById('open-backup').onclick=openBackup;document.getElementById('backup-file').onchange=inspectBackup;document.getElementById('backup-form').onsubmit=restoreBackup;document.querySelectorAll('[data-close-backup]').forEach(button=>button.onclick=()=>backup.close());document.getElementById('edit-form').onsubmit=saveCollection;document.querySelectorAll('[data-close-edit]').forEach(button=>button.onclick=()=>edit.close());document.getElementById('close-library').onclick=closeLibrary;document.getElementById('library-back').onclick=browserBack;document.getElementById('library-search').oninput=event=>{clearTimeout(searchTimer);browser.query=event.target.value.trim();browser.scope=browser.query?'all':browser.scope;document.querySelectorAll('[data-scope]').forEach(button=>button.classList.toggle('active',button.dataset.scope===browser.scope));searchTimer=setTimeout(()=>loadBrowser(),450)};document.getElementById('clear-library-search').onclick=()=>{document.getElementById('library-search').value='';browser.query='';loadBrowser()};document.getElementById('library-sort').onchange=event=>{browser.sort=event.target.value;loadBrowser()};document.getElementById('scope-tabs').querySelectorAll('[data-scope]').forEach(button=>button.onclick=()=>setScope(button.dataset.scope));document.getElementById('clear-selection').onclick=()=>{selected.clear();browser.albumDetail?renderAlbumDetail():renderBrowser()};document.getElementById('add-selected').onclick=addSelected;library.addEventListener('cancel',()=>{clearTimeout(searchTimer);browser.controller?.abort();browser.generation+=1});library.addEventListener('keydown',event=>{if(event.key==='Escape'){event.preventDefault();closeLibrary()}});audio.addEventListener('play',updatePreviewIndicators);audio.addEventListener('pause',updatePreviewIndicators);audio.addEventListener('ended',updatePreviewIndicators);audio.addEventListener('timeupdate',updatePlayerTime);audio.addEventListener('loadedmetadata',updatePlayerTime);audio.addEventListener('error',previewError);document.getElementById('player-toggle').onclick=()=>audio.paused?audio.play().catch(previewError):audio.pause();document.getElementById('player-back').onclick=()=>audio.currentTime=Math.max(0,audio.currentTime-10);document.getElementById('player-forward').onclick=()=>audio.currentTime=Math.min(audio.duration||Infinity,audio.currentTime+10);document.getElementById('player-close').onclick=stopPreview;document.getElementById('player-seek').oninput=event=>{if(Number.isFinite(audio.duration))audio.currentTime=Number(event.target.value)/1000*audio.duration};
refreshCollections().catch(error=>{list.innerHTML=`<p class="empty">${esc(error.message)}</p>`});
})();
</script>
"""
    return html.replace("__PRINCIPAL__", escape(principal_label)).replace(
        "__PRINCIPAL_DETAIL__", escape(principal_detail)
    )
